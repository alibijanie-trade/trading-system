# 📋 PENDING FOR NEXT VERSION — مخزن موقت تغییرات v2.X → v2.X+1

> **هدف یک‌خطی:** ذخیره real-time هر [PENDING-EOC] که در طول چت ثبت می‌شود، تا در پایان چت به سند جامع v(X+1) ادغام شود و گم نشود.

> **محل قرارگیری:** `docs/PENDING_FOR_NEXT_VERSION.md`  
> **مرجع قانون:** قانون پیشنهادی #۶۰ (چت ۸)  
> **نسخه فعلی:** v2.9 → در حال جمع‌آوری برای v2.10  
> **شروع جمع‌آوری:** چت ۸ — `TRADING-phase0-part08-pre-phase1-setup` (2026-05-19)  
> **آخرین به‌روزرسانی:** 2026-05-19 (تولد فایل)

---

## 🚨 بخش ۰ — هشدار به Claude در شروع هر چت

> **اگر در چت‌های آینده این فایل را می‌بینی:**
> یعنی PENDING-EOC هایی از چت قبل **نیمه‌تمام** مانده‌اند. وظیفه تو:
>
> 1. **محتوای کامل این فایل را بخوان** (با Filesystem MCP، قبل از هر کار دیگر)
> 2. **با کاربر تأیید کن** کدام موارد باید به سند جامع ادغام شوند (می‌توان همه را یک‌جا، یا انتخابی)
> 3. **پس از ادغام موفق** در v(X+1):
>    - بخش‌های ۱-۶ این فایل را پاک کن
>    - فقط هدر + بخش ۰ + بخش ۷ (پروتکل) + بخش تاریخچه ادغام را نگه دار
>    - یک خط جدید در «تاریخچه ادغام‌ها» بزن: `آخرین ادغام: v2.X → v2.X+1 در چت N (تاریخ Y) — تعداد موارد: Z`
> 4. **هرگز این فایل را delete نکن** — همیشه به‌عنوان draft زنده باقی می‌ماند

⛔ **اگر این فایل بزرگ شد ولی محتوا ادغام نشد، یعنی یک چت بدون پایان رسمی بسته شده.** این علامت قرمز است — به کاربر اطلاع بده.

---

## 📋 بخش ۱ — پیشنهاد رسمی قانون #۶۰

فرمت: مطابق سند ۱۳.۵.۴ (الزامات فرمت پیشنهاد)

```
📋 پیشنهاد بهبود:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
سند مرتبط:    سند ۱ — قوانین همکاری | بخش ۱.۹ (جدول قوانین قفل‌شده)
              + سند ۱۶.۵ (چک‌لیست پایان چت) + سند ۱۸ (درس‌نامه)
سطح:          🔒 پیشنهاد قفل‌شدن (پیش‌تر در حد 💡 پیشنهادی)
وضعیت فعلی:   PENDING-EOC ها در حافظه چت ذخیره می‌شوند و در پایان
              چت گم می‌شوند. مثال عینی: چت ۷ (M23).
پیشنهاد:      ثبت لحظه‌ای هر [PENDING-EOC] در فایل
              `docs/PENDING_FOR_NEXT_VERSION.md` با Filesystem MCP،
              بلافاصله پس از کشف — نه «بعداً اضافه می‌کنم».
دلیل:         حل ریشه‌ای M23 (مهم‌ترین درس کل پروژه). تجربه چت ۷
              نشان داد که حتی با وجود قوانین #۲۳-۲۶، اگر ثبت در
              فایل نباشد، فقط در حافظه می‌ماند و گم می‌شود.
مزیت:         (۱) صفر گم‌شدن PENDING-EOC
              (۲) source-of-truth واحد در پایان چت (دیگر تناقض اعداد ۴۸/۵۹/۵۲)
              (۳) Claude بعدی می‌تواند بفهمد چه چیزی نیمه‌تمام مانده
              (۴) audit trail شفاف برای ادغام در سند جامع
اثرات:        - CLAUDE_CHECKLIST.md فاز ۲: قانون جدید «ثبت لحظه‌ای PENDING در فایل»
              - سند ۱۸.۳: مرجع به این فایل به‌عنوان مکمل بخش ۱۸
              - جدول ۱.۹: ردیف جدید #۶۰
              - سند ۱۶.۶ (Atomic Updates): این فایل هم باید جزو
                اسناد به‌روزرسانی پایان چت باشد
ریسک:         کم — overhead زمانی هر write با MCP کمتر از ۲ ثانیه.
              تنها ریسک: فراموش کردن خود قانون #۶۰ — راه‌حل: قانون #۴۸
              این فایل را اجباری در شروع هر چت می‌خواند.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
آیا این تغییر تأیید می‌شود؟ [در چت ۸ توسط صاحب پروژه]
```

---

## 📜 بخش ۲ — قوانین جدید پیش‌نویس (برای جدول ۱.۹)

این قوانین در چت‌های گذشته پیشنهاد شدند ولی هنوز در سند جامع رسماً ثبت نشده‌اند. در v2.10 به جدول ۱.۹ منتقل می‌شوند.

| # | قانون قفل‌شده پیش‌نویس | چت کشف |
|---|---|---|
| **#۴۸** | پروتکل اجباری خواندن بخش ۱۸ (درس‌نامه M ها) و `PENDING_FOR_NEXT_VERSION.md` در شروع هر چت — قبل از هر کار دیگر. مرجع: سند v2.9 بخش ۱۸.۳ + سیاست جدید چت ۸ (افزوده شده تا هیچ درسی، چه ثبت‌شده چه pending، گم نشود). | چت ۷ (planning) + چت ۸ (refinement) |
| **#۴۹** | Filesystem MCP — read-only tools → Always Allow؛ write/delete/copy → Needs Approval؛ هرگز `.env` تغییر نده؛ هرگز خارج پوشه پروژه عمل نکن. | چت ۷ |
| **#۵۰** | با فعال‌سازی Filesystem MCP، متن handoff شروع چت کوتاه می‌شود. به‌روزرسانی سند ۱۷.۱ (Template اولین پاسخ). | چت ۷ |
| **#۵۱** | قبل از هر write/delete با Filesystem MCP، تأیید صریح کاربر گرفته شود (مکمل قانون #۴۹). | چت ۷ |
| **#۵۲** | ⚠️ **gap** — در جستجوی Memory ظاهر نشد. در صورت کشف بعدی، اینجا ثبت شود. | چت ۷ |
| **#۵۳** | ⚠️ **gap** — در جستجوی Memory ظاهر نشد. | چت ۷ |
| **#۵۴** | "Allow network egress" در Claude sandbox صرفاً برای pip/npm/httpx است — هرگز روی project local تأثیر ندارد. | چت ۷ |
| **#۵۵** | اگر فایل Project Knowledge آپدیت شد، Claude باید نسخه جدید را تولید کند تا کاربر در Project Knowledge جایگزین کند. | چت ۷ |
| **#۵۶** | مسیر تحویل فایل از Claude همیشه با پوشه مشخص اعلام شود (artifact برای download، یا کپی به `claude_workspace/`، یا مسیر مشخص دیگر). | چت ۷ |
| **#۵۷** | screenshots آپلودی کاربر → `claude_workspace/screenshots/` (با gitignore). | چت ۷ |
| **#۵۸** | Snapshot هنگام آپدیت Project Knowledge → `claude_workspace/snapshots/` (tracked در git برای تاریخچه). | چت ۷ |
| **#۵۹** ⭐ | **بلااستثنا** اعلام مسیر دانلود برای هر فایل که Claude تولید می‌کند — تأکید قاطع کاربر، مرجع چت ۷. | چت ۷ |
| **#۶۰** ⭐⭐⭐ | **PENDING-EOC در لحظه ثبت** — هر [PENDING-EOC] که در چت ثبت می‌شود، باید **همان لحظه** با Filesystem MCP در `docs/PENDING_FOR_NEXT_VERSION.md` نوشته شود. آخر چت، این فایل به سند v(X+1) ادغام می‌شود و خالی می‌شود. علت: جلوگیری از گم شدن (درس M23). | چت ۸ |
| **#۶۱** ⭐ | **پیشنهاد گزینه مطلوب در سؤالات چندگزینه‌ای** — وقتی Claude سؤال چندگزینه‌ای می‌پرسد یا چند گزینه برای انتخاب ارائه می‌دهد، باید **پیشنهاد مطلوب خود** را هم صریح بگوید: گزینه‌ای که از نظر حرفه‌ای، انسجام پروژه، و بلندمدت بهتر است. کاربر می‌تواند مخالف باشد، ولی نباید مجبور باشد بدون راهنمایی Claude تصمیم بگیرد. شامل سؤالات A/B/C، رتبه‌بندی، multi-select، و درخواست‌های open-ended که چند مسیر دارند. | چت ۸ |

---

## 🎓 بخش ۳ — درس‌نامه‌های جدید پیش‌نویس (برای جدول ۱۸.۲)

این درس‌ها در چت‌های گذشته شناسایی شدند ولی در سند v2.9 ثبت نشدند. در v2.10 به سند ۱۸ منتقل می‌شوند.

| # | اشتباه | علت ریشه‌ای | راه‌حل آینده | چت کشف |
|---|---|---|---|---|
| **M22** | ⚠️ **gap** — آخرین رسمی در سند v2.9 = M21. در snippet های چت ۷ شماره‌های بزرگ‌تر ذکر شده ولی محتوای M22 مشخص نشد. | — | — | چت ۷ |
| **M23** ⭐⭐⭐ | Claude در پایان چت ۷ پیام handoff به چت بعدی **تولید نکرد**، با وجود نوشتن قانون آن در همان چت. | بر اساس حافظه فعال کار کرد، نه چک‌لیست فعالانه سند ۱۷.۵. اعتماد به حافظه به‌جای فایل. | فرآیند جدید (قانون #۶۰): PENDING در فایل نوشته شود تا گم نشود. **مهم‌ترین درس کل پروژه — اگر این حل نشود، همه قوانین فقط روی کاغذ هستند.** | چت ۷ |
| **M24** | ⚠️ **gap** — در جستجوی Memory ظاهر نشد. | — | — | چت ۷ |
| **M25** | پاسخ به سؤالات misunderstanding با تأیید ساده، بدون مثال تفاوت‌ها. | فرض درست‌بودن سؤال کاربر | تفاوت‌ها را با مثال نشان بده، نه فقط تأیید کن. مثال: «MCP ≠ حافظه بین چت‌ها». | چت ۷ |
| **M26** | اطلاعات نصب Filesystem MCP از حافظه (نه web_search). | فرض به‌روز بودن دانش Claude | web_search برای موارد سریع‌تغییر (Claude Desktop features، نسخه extension ها). | چت ۷ |
| **M27** | پس از کار install/configure، چک‌لیست post-install ارائه نشد. | فقدان pattern استاندارد | پس از هر install/configure، چک‌لیست تأیید (verification checks) ارائه شود. | چت ۷ |
| **M28** | فرض «Enable توگل = Configure شد». | عدم تشخیص دو مرحله جدا | Enable toggle ≠ Configure (تعیین پوشه) — دو گام مستقل. در راهنمای نصب هر دو گام جدا توضیح داده شود. | چت ۷ |
| **M29** | ⚠️ **gap** — در جستجوی Memory ظاهر نشد. | — | — | چت ۷ |
| **M30** ⭐ | فرض «Filesystem MCP در همین چت کار خواهد کرد». | عدم اطلاع از reload mechanism | MCP فقط در چت‌های **جدید** load می‌شود، نه چت جاری. هر extension/connector جدید باید در چت جدید تست شود. | چت ۷ |
| **M31** | پاسخ به «چه قابلیت‌های دیگری وجود دارد» از حافظه (بدون search). | فرض به‌روز بودن قابلیت‌ها | web_search قبل از پاسخ به سؤالات «چه قابلیتی…». | چت ۷ |
| **M32-M43** | ⚠️ **gap** — این بازه در جستجوی Memory ظاهر نشد. | — | — | چت ۷ |
| **M44** | فراموش کردن یادآوری به کاربر که Filesystem MCP در چت قبلی در دسترس نیست. | فرض اطلاع کاربر از M30 | با هر سؤال «این کار را انجام بده» در چت‌های پیش از MCP، یادآوری محدودیت سیستمی. | چت ۷ |
| **M45-M55** | ⚠️ **gap** — این بازه در جستجوی Memory ظاهر نشد. | — | — | چت ۷ |
| **M56** | تکیه به Memory برای بازیابی اعداد دقیق و لیست‌های کامل. | Memory برای جستجوی مفهومی خوب است، نه ثبت دقیق. | برای ثبت دقیق، **فایل** لازم است (نه فقط Memory). این درس مبنای قانون #۶۰ شد. | چت ۸ |
| **M57** | تناقض اعداد در پایان چت (۴۸ vs ۵۹ vs ۵۲ برای قوانین). | بازشمارش بدون مرجع واحد در پایان چت. | پایان چت همیشه از یک فایل **source-of-truth** (همین فایل) استفاده شود. | چت ۸ |
| **M58** | متن handoff به‌صورت inline در پیام (نه فایل دانلودی) — کاربر گمان کرد فایل قابل دانلود وجود دارد. | عبارت مبهم «این را استفاده کنید». | یا صریح بگو «این **متن** را copy کنید»، یا فایل قابل دانلود تولید کن. | چت ۸ |
| **M59** | موارد جدید (قانون، درس، تصمیم) را **خارج** از متن paste گفتن، نه داخل آن. | فرض دیدن همه context چت توسط Claude بعدی. | هر موضوع جدید باید **داخل** متن copy/paste باشد، چون Claude بعدی فقط همان متن را می‌بیند. | چت ۸ |
| **M60** ⭐ | Write مستقیم فایل بزرگ/مهم با MCP بدون preview ساختار. | عجله یا فرض درست بودن طراحی Claude. | قبل از هر write فایل بزرگ/مهم با MCP، ساختار preview شود و تأیید کاربر گرفته شود. این کار: (۱) ریسک data loss را کاهش می‌دهد (۲) کاربر می‌تواند ساختار را اصلاح کند قبل از commit (۳) در صورت اشتباه، rollback آسان‌تر است. **همین فایل اولین تست عملی این درس است.** | چت ۸ |
| **M61** | اعتماد به status موفقیت کافی نیست (write/install/configure که موفق گزارش می‌شود). | فرض = «گزارش موفق ⇒ نتیجه درست». مثال: M1 (اسکریپت ۴۸ گزارش updated داد ولی فایل به‌روز نشد). | همیشه read-back / verify مستقل پس از هر اقدام. چرخه کامل safety: **Preview → Approve → Write → Read-back → Verify → Confirm**. مکمل M1 و M60. | چت ۸ |
| **M62** | Claude در پیامی قانون #۳۷ را ذکر کرد، ولی در پاسخ بعدی همان چت کاربر باید یادآوری کند که read-back لازم است. | قوانینی که خود Claude ذکر می‌کند priority پایین‌تری از یادآوری کاربر می‌گیرند (نقض self-enforcement). | قوانینی که در همان چت توسط Claude ذکر می‌شوند، باید **خودکار** در باقی همان چت اجرا شوند — priority بالاتر از یادآوری کاربر. این یک self-binding است: اگر Claude قانونی را ذکر کند، در همان چت ملزم به اجرای آن است. | چت ۸ |

---

## 🆕 بخش ۴ — بخش‌های پیشنهادی جدید سند جامع v2.10

این بخش‌ها در v2.10 به سند جامع اضافه می‌شوند:

- **سند ۲۲: Filesystem MCP Integration** — راهنمای کامل (مرحله نصب، configure پوشه، tools available، permissions tuning، read/write workflow، استثنائات مثل `.env`، troubleshooting).
- **سند ۲۳: Claude Desktop Configuration** — Memory toggles (Search and reference chats + Generate memory from chat history)، Project Knowledge، Custom Instructions، Settings → Capabilities (با مرجع به Settings audit کامل در چت ۸).
- **سند ۲۴: `claude_workspace/` Structure** — ۵ subfolder (`incoming_permanent/`, `old_versions/`, `screenshots/`, `snapshots/`, `zip_temp/`) + gitignore policy + مرجع به قوانین #۵۶-۵۹.
- **سند ۲۵: Skills اختصاصی پروژه (placeholder)** — پس از فاز ۱ ساخته می‌شود. شامل دستورالعمل‌های خاص پروژه برای Claude Code و Skills system.

---

## ⚠️ بخش ۵ — شکاف‌های شناخته‌شده (Known Gaps)

این موارد در حافظه چت ۷ بودند ولی در جستجوی Memory در چت ۸ ظاهر نشدند:

**قوانین گم‌شده:**
- #۵۲, #۵۳

**درس‌های گم‌شده:**
- M22 (آخرین placeholder رسمی)
- M24, M29
- M32 تا M43 (۱۲ مورد)
- M45 تا M55 (۱۱ مورد)

**سیاست:** در v2.10 این موارد را به‌عنوان `Reserved / Unknown` در جدول می‌گذاریم — تا اگر در چت‌های بعدی پیدا شدند، جای آن‌ها مشخص باشد. این رویکرد "conservative numbering" — مثل CVE numbers.

**روش‌های بازیابی احتمالی در آینده:**
- جستجوی عمیق‌تر در Memory با query های هدفمند (مثل auth، theme، migration، snapshot، …)
- مرور `CHAT_LOG.md` چت ۷ (اگر بعداً تکمیل شد — اکنون فقط stub دارد)
- بازخوانی commit history چت ۷ (`91704ca` → `d6bc75c`) با git log

---

## 📊 بخش ۶ — آمار جمع‌آوری

| دسته | کشف‌شده | gap | کل ردیف |
|---|---|---|---|
| قوانین جدید | ۱۱ (#۴۸-۵۱، ۵۴-۶۱) | ۲ (#۵۲، #۵۳) | ۱۳ |
| درس‌نامه جدید | ۱۵ (M23، M25-M28، M30، M31، M44، M56-M62) | ۲۵ (M22، M24، M29، M32-M43، M45-M55) | ۴۰ |
| بخش‌های جدید سند | ۴ (سند ۲۲-۲۵) | ۰ | ۴ |

**درصد تخمینی کامل بودن:**
- 🟢 **قوانین: ~۸۵%** (۱۱ از ۱۳)
- 🟡 **درس‌نامه: ~۳۸%** (۱۵ از ۴۰)
- 🟢 **بخش‌های جدید: ۱۰۰%**

**درجه اعتماد به ادغام v2.10:**
- قوانین: **بالا** — می‌توان با اطمینان ادغام کرد
- درس‌نامه: **متوسط** — gap های قابل‌توجه، احتمال کشف بیشتر در آینده
- بخش‌های جدید: **بالا** — اسکلت کافی است، جزئیات در حین نوشتن پر می‌شود

---

## 🔄 بخش ۷ — پروتکل به‌روزرسانی این فایل (قانون #۶۰)

**سه قانون عملیاتی برای Claude:**

### قانون ۷.۱ — ثبت لحظه‌ای

هر زمان که در طول چت یک `[PENDING-EOC]` کشف یا پیشنهاد شد:

1. **همان لحظه** با MCP `edit_file` این فایل، آن را در بخش مناسب (۲ یا ۳ یا ۴) اضافه کن
2. شماره ردیف صحیح را انتخاب کن (ادامه آخرین، نه پر کردن gap)
3. تاریخ + شماره چت در ستون «چت کشف» ثبت شود
4. **هرگز** «بعداً اضافه می‌کنم» نگو — اگر اضافه نشود، فراموش می‌شود

### قانون ۷.۲ — read-back verify (قانون #۳۷)

پس از هر write به این فایل، **حتماً** با MCP فایل را دوباره بخوان و تأیید کن محتوا درست ثبت شده. در صورت اختلاف، مجدد اصلاح کن.

### قانون ۷.۳ — ادغام در پایان چت

در پایان هر چت (پس از تأیید صریح کاربر برای پایان — قانون #۲۷):

1. کاربر تأیید می‌کند کدام موارد ادغام شوند
2. اسکریپت Python idempotent بنویس که محتوای این فایل را به سند جامع v(X+1) ادغام کند
3. پس از ادغام موفق + verify:
   - بخش‌های ۱-۶ این فایل را پاک کن
   - هدر + بخش ۰ + بخش ۷ + بخش تاریخچه ادغام را نگه دار
   - یک خط لاگ در پایان «تاریخچه ادغام‌ها» بزن
4. این فایل را در همان commit پایان چت با git track کن

---

## 📜 تاریخچه ادغام‌ها

(این لیست در طول زندگی پروژه رشد می‌کند)

- _هیچ ادغامی هنوز انجام نشده._ اولین ادغام: پایان چت ۸ → v2.10.

---

## 🆕 بخش ۸ — PENDING برای v2.11 (در حال جمع‌آوری در چت ۸ پس از ادغام v2.10)

### آیتم B5.1 — اصلاح فرمت `.gitignore` برای `claude_workspace/`

**کشف:** چت ۸، بلوک ۱-B (B5 Infrastructure audit) — 2026-05-19  
**سطح:** 💡 پیشنهاد / minor (در عمل کار می‌کند، فقط فرمت پیشنهاد سند ۲۴.۳ متفاوت است)

**وضعیت فعلی در `.gitignore`:**
```gitignore
claude_workspace/zip_temp/
claude_workspace/screenshots/
```

**پیشنهاد طبق سند جامع v2.10 بخش ۲۴.۳:**
```gitignore
claude_workspace/screenshots/*
!claude_workspace/screenshots/.gitkeep
claude_workspace/zip_temp/*
!claude_workspace/zip_temp/.gitkeep
```

**علت:** فرمت پیشنهاد سند ۲۴.۳ صریح‌تر است — همه فایل‌های داخل پوشه را ignore می‌کند ولی `.gitkeep` را حفظ. فرمت فعلی هم در عمل کار می‌کند (چون `.gitkeep` ها قبلاً tracked شده‌اند)، ولی اگر کسی پوشه را حذف و دوباره ساخت، باید `.gitkeep` را force-add کند.

**ادغام در:** v2.11 (low priority)  
**ثبت‌شده توسط:** قانون #۶۰

---

### آیتم B2.1 — اصلاح سند ۲۳.۱: مسیر Memory toggles

**کشف:** چت ۸، بلوک ۲ (Settings audit) — 2026-05-19  
**سطح:** 🎯 تصمیم‌شده / important (اطلاعات سند اشتباه است)

**وضعیت فعلی در سند ۲۳.۱:**
```
در Settings → Profile → Memory:
```

**واقعیت در نسخه فعلی Claude Desktop (تأیید با screenshot کاربر):**
```
در Settings → Capabilities → Memory:
```

**پس از ادغام در v2.11 باید اصلاح شود:**
```
در Settings → Capabilities → بخش Memory (نه Profile):
```

**توضیح Memory toggles در نسخه فعلی:**
- **Search and reference chats** — جستجو در چت‌های قبلی
- **Generate memory from chat history** — Claude هم در چت هم در Project memory می‌سازد
- **Chat memory** (بخش نمایش): نشان می‌دهد چه دانشی الان ذخیره شده
- **Import memory from other AI providers** — فیچر جدید

**توجه:** در screenshot کاربر ‌**Incognito mode** در Memory tab وجود ندارد — احتمالاً این toggle الآن در جای دیگری است (شاید در چت flow اجتماعی، یا فعلاً ارائه نمی‌شود در نسخه فعلی).

**علت اشتباه اولیه:** دانش Claude درباره Claude Desktop UI از تراینینگ cutoff بوده، ولی Claude Desktop به‌صورت مداوم به‌روز می‌شود. این درس M26 (web_search برای موارد سریع‌تغییر) را تأیید می‌کند.

**ادغام در:** v2.11 (important)  
**ثبت‌شده توسط:** قانون #۶۰

---

### آیتم C1.1 — نتایج Settings Audit (چت ۸ بلوک ۲)

**کشف:** چت ۸، بلوک ۲ (Settings audit با ۳ screenshot) — 2026-05-19  
**سطح:** 📊 ثبت وضعیت — برای افزودن به سند ۲۳.۴

**Settings → Capabilities (تأیید‌شده توسط سیستم):**

| Toggle | وضعیت | پیشنهاد سند ۲۳.۴ | وضعیت |
|---|---|---|---|
| Search and reference chats | ✅ ON | ✅ ON | درست |
| Generate memory from chat history | ✅ ON | ✅ ON | درست |
| Connector discovery | ❌ OFF | (ذکر نشده) | امن (پیشفرض بهتر) |
| Artifacts | ✅ ON | ✅ ON | درست |
| AI-powered artifacts | ❌ OFF | (ذکر نشده) | لازم نیست |
| Inline visualizations | ✅ ON | (ذکر نشده) | خوب |
| Cloud code execution | ✅ ON | ✅ ON | درست |
| Allow network egress | ✅ ON | ✅ ON | درست |
| Domain allowlist | Package managers only | (مستند نشده) | امن (پیشنهاد: افزودن به سند ۲۳.۴) |

**پیشنهاد برای افزودن در سند ۲۳.۴ (v2.11):**
- ردیف `Connector discovery` (پیشنهاد OFF — امن‌تر)
- ردیف `AI-powered artifacts` (پیشنهاد OFF — در فاز ۰ لازم نیست)
- ردیف `Inline visualizations` (پیشنهاد ON — مفید برای Mermaid diagrams)
- بخش `Domain allowlist` (پیشنهاد: Package managers only — توضیح چرا)

**ادغام در:** v2.11 (medium priority)  
**ثبت‌شده توسط:** قانون #۶۰

---

### آیتم D1.1 — Bug: pre-commit hook `end-of-file-fixer` با نام فایل فارسی روی Windows کرش می‌کند ⭐⭐

**کشف:** چت ۸، بلوک ۲ (D1 GitHub setup، اولین commit پس از v2.10) — 2026-05-19  
**سطح:** 🔴 important / bug در tooling

**علائم:**
```
fix end of files.........................................................Failed
- hook id: end-of-file-fixer
- exit code: 1
- files were modified by this hook
UnicodeEncodeError: 'charmap' codec can't encode characters in position 12-14
File "...\pre_commit_hooks\end_of_file_fixer.py", line 64, in main
    print(f'Fixing {filename}')
  File "C:\Program Files\Python311\Lib\encodings\cp1252.py"
```

**علت ریشه‌ای:**
- PowerShell ویندوز default encoding = `cp1252`
- نام فایل `سند_جامع_v2_10.md` شامل حروف فارسی است
- پکیج `pre-commit-hooks` (نسخه فعلی) از `print(f'Fixing {filename}')` بدون handling برای non-ASCII filenames استفاده می‌کند
- نکته: کار اصلی hook (افزودن newline) **انجام شد** — فقط print کرش کرد
- این مشابه قانون #۴۶ است ولی در کد third-party (نه کد ما)

**Workaround موقت (استفاده شد در چت ۸):**
```powershell
$env:PYTHONIOENCODING = "utf-8"
git add docs/ claude_workspace/
git commit -m "..."
```

**راه‌حل دائمی پیشنهادی برای v2.11:**
1. **Option A — متغیر محیطی دائمی Windows:**
   ```
   [Environment]::SetEnvironmentVariable("PYTHONIOENCODING", "utf-8", "User")
   ```
   (یک‌بار اجرا، در همه session های آینده فعال)

2. **Option B — افزودن به `.pre-commit-config.yaml`:**
   ```yaml
   default_language_version:
     python: python3.11
   # ...
   ```
   و در runtime استفاده از env vars

3. **Option C — فقط در سند تست‌شده/مستند کنیم** که `PYTHONIOENCODING=utf-8` در `.bashrc` / Windows env vars لازم است

**درس قابل استخراج (M63+):**
- پکیج‌های third-party Python ممکن است در پروژه‌های با نام فایل non-ASCII روی Windows کرش کنند
- `PYTHONIOENCODING=utf-8` باید default در راهنمای onboarding باشد
- قانون #۴۶ ASCII-only فقط برای کد ما کار می‌کند، نه third-party

**ادغام در:** v2.11 (important)  
**ثبت‌شده توسط:** قانون #۶۰

---

### آیتم E2.1 — Bug #50 root cause + راه‌حل cleanup (T2.13 تحلیل)

**کشف:** چت ۸، بلوک ۳ (E2 — T2.13 root cause analysis) — 2026-05-19  
**سطح:** 🟡 medium / cleanup task (غیر-urgent — همه تست‌ها pass هستند با workaround)

**وضعیت فعلی:**
- ۱۵ فایل `.jsx` دارای خط `import React from 'react';` هستند (از چت ۶ script 45)
- `vite.config.js`: `plugins: [react()]` در top-level
- همه ۳۰ تست vitest ✅ pass
- Bug وضعیت پایدار دارد

**تحلیل علت ریشه‌ای:**

Stack: React 19.2.6 + @vitejs/plugin-react 6.0.1 + Vite 8.0.12 + Vitest 3.2.4

از React 17+، plugin-react به‌صورت پیش‌فرض باید از **automatic JSX runtime** استفاده کند (که نیاز به `import React` ندارد). اما در ترکیب vitest 3.2 + vite 8 + plugin-react 6:

1. plugin-react v6 تنظیم صریح `jsxRuntime: 'automatic'` ندارد
2. در محیط jsdom + vitest، بدون صریح‌سازی، classic runtime فعال می‌شود
3. در classic runtime، JSX به `React.createElement(...)` ترجمه می‌شود → نیاز به `import React`

**راه‌حل پیشنهادی (برای چت ۹):**

```javascript
// frontend/vite.config.js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react({
    jsxRuntime: 'automatic',  // ⬅️ صریح‌سازی
  })],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test/setup.js'],
    css: false,
    coverage: { /* ... */ },
  },
})
```

پس از تغییر:
1. `npm test` — بررسی کن تست‌ها هنوز pass هستند با تغییر تنها
2. از ۱۵ فایل `.jsx`، خط `import React from 'react';` حذف شود — در صورتی که React فقط برای JSX استفاده شده باشد (نه برای `React.useEffect`، `React.memo`، `React.forwardRef`، ...)
3. `npm test` دوباره — همه تست‌ها باید پاس بمانند

**فایل‌های تحت تأثیر (کشف‌شده در چت ۶ script 45):**
- `App.jsx` ، `main.jsx`
- همه `pages/*.jsx` (LoginPage ، HomePage ، ChartPage ، SettingsPage ، ...)
- همه `components/common/*.jsx` (Toast ، ConfirmDialog ، ErrorBoundary ، ProtectedRoute ، ThemeProvider ، SkeletonBlock)
- تعداد دقیق: ۱۵ فایل

**توجه مهم:**
- این به‌عنوان cleanup است، نه fix
- Bug Bug نیست — همه تست‌ها pass هستند
- فقط حذف کد تکراری + ترازی‌سازی با best practice React 19 است

**ادغام در:** v2.11 یا فاز ۱ (low-medium priority — تلفیق با اولین تغییر frontend)  
**ثبت‌شده توسط:** قانون #۶۰

---

### آیتم D2.1+D3.1 — Claude Code + GitHub MCP ارزیابی (T2.12)

**کشف:** چت ۸، بلوک ۳ (D2 + D3) — 2026-05-19  
**سطح:** 🟢 low / ارزیابی برای آینده (غیر-urgent)

**Claude Code (D2):**
- اجازه می‌دهد Claude مستقیم در terminal کار کند بدون paste چت
- مزیت: بدون workflow zip، git aware native، MCP integration بهتر
- عیب: کاربر دانش CLI لازم دارد — برای پروژه فعلی که کاربر غیربرنامه‌نویس است مناسب نیست
- **پیشنهاد:** در فاز ۴+ (بک‌تست engine) وقتی پروژه پیچیده‌تر می‌شود، دوباره ارزیابی شود

**GitHub MCP Connector (D3):**
- دسترسی Claude به GitHub APIs (read repo، open PR، manage issues)
- مزیت: بازیابی commit history بدون local، ساخت PR در فاز ۱+
- عیب: تداخل با Filesystem MCP (overlapping)، ریسک امنیتی اگر PAT scope باز باشد
- **پیشنهاد:** در فاز ۵+ (security hardening) یا وقتی تیم بزرگ شد. در فاز ۰+۱ با Filesystem MCP کافی است.

**تصمیم:** T2.12 باقی می‌ماند در Tier 3 (low priority، XL). در فاز ۴+ دوباره بررسی شود.

**ادغام در:** تصمیم فاز ۴+ (فعلاً document در PENDING می‌ماند)  
**ثبت‌شده توسط:** قانون #۶۰

---

### آیتم G5.1 — پیام handoff باید فایل دائمی باشد

**کشف:** چت ۸، پایان چت (گفتگوی پایانی با کاربر) — 2026-05-19  
**سطح:** 🔴 important / UX critical

**مشکل کشف‌شده:**
- در چت ۸، متن handoff فقط در پیام چت Claude بود
- کاربر برای دسترسی به آن در آینده (مثلا بعد از close کردن چت) راهی ندارد
- مشابه M58 (متن inline vs فایل دانلودی) — ولی در مورد handoff message

**راه‌حل پیشنهادی برای v2.11:**

افزودن قانون جدید (پس از #۶۱):

```
**#۶۲** ⭐ v2.11 — **فایل handoff دائمی پایان چت** —
Claude در پایان هر چت باید پیام handoff را به‌صورت فایل دائمی در:
`claude_workspace/incoming_permanent/CHAT{N+1}_HANDOFF.txt`
ذخیره کند تا کاربر در آینده بتواند به آن دسترسی داشته باشد.
متن inline در پیام چت به تنهایی کافی نیست (مشکل کشف‌شده در پایان چت ۸).
```

و افزودن به CLAUDE_CHECKLIST فاز ۳ مرحله ۱۲.۵:
«تولید فایل CHAT{N+1}_HANDOFF.txt در `claude_workspace/incoming_permanent/`»

**اعمال عملی در چت ۸:** فایل `CHAT9_HANDOFF.txt` ساخته شد و در commit بعدی به GitHub می‌رود.

**ادغام در:** v2.11 (important)  
**ثبت‌شده توسط:** قانون #۶۰

---

### آیتم UX1.1 — Convention بصری برای گام‌های اجرایی کاربر

**کشف:** چت ۸، پایان چت (درخواست کاربر) — 2026-05-19  
**سطح:** 🎯 important / UX  
**درخواست کاربر:** «لطفا شروع گامهای اجرایی را با یک رنگ خاص نشون بده...»

**راه‌حل پیشنهادی:**

افزودن قانون جدید (پس از G5.1):

```
**#۶۳** ⭐ v2.11 — **Convention بصری برای گام‌های اجرایی** —
هر گام اجرایی که نیاز به کپی-پیست/اجرای فرمان توسط کاربر دارد،
باید با `▶️ **EXECUTE — اقدام لازم:**` شروع شود و با خط جداکننده
`---` در بالا و پایین احاطه شود. دلیل: تشخیص بصری سریع برای کاربر
وقتی نیاز به توضیحات ندارد و فقط می‌خواهد فرمان‌ها را اجرا کند.
```

**مثال صحیح:**

```
توضیحات مفصل درباره کار...

---

▶️ **EXECUTE — اقدام لازم:**

```powershell
git status
```

---

توضیحات بعدی...
```

**افزودن به سند بخش ۱.۹ و سند ۸.۹ (استانداردهای فرم)**

**تأیید کاربر:** در پایان چت ۸ تأیید شد ✅ — با درخواست رنگ سبز تیتر (🟢 یا ✅)

**فرمت نهایی تأیید‌شده:**
```markdown
## 🟢 ▶️ EXECUTE — اقدام لازم

🟦 **tab «1 backend»** (یا 🟩 «2 scripts»، 🟧 «3 frontend»، 🟥 «4 BACKUP»)

​```powershell
[دستور]
​```
```

**ادغام در:** v2.11 (important)  
**ثبت‌شده توسط:** قانون #۶۰

---

### آیتم UX2.1 — عدم نمایش جزئیات تصحیح خطای کد

**کشف:** چت ۸، پایان چت (درخواست کاربر) — 2026-05-19  
**سطح:** 🎯 important / UX  
**درخواست کاربر:** «لازم نیست به من نشون بدی که برای اصلاح کدهای مشکل دار کار کردی یا توضیح بدی دلیل اینکه خطا داد چی بوده. فقط گامهای اجرایی برای من بیار.»

**راه‌حل پیشنهادی:**

افزودن قانون جدید (پس از #۶۳):

```
**#۶۴** ⭐ v2.11 — **عدم نمایش جزئیات تصحیح خطای کد** —
وقتی کد اشتباه می‌شود و Claude باید آن را اصلاح کند، باید:
(ا) علت را عمیق در ذهن خود بررسی کند
(ب) **توضیحات چرایی خطا و روش تشخیص را به کاربر نشان ندهد**
(ج) فقط گام اجرایی کد اصلاحی را با Convention #۶۳ (▶️ EXECUTE) ارائه دهد
(د) شامل نام tab + رنگ tab مطابق قانون #۳۱

استثنائات (توضیح کامل لازم است):
- گزارش کارهای انجام‌شده
- گزارش کارهای آینده
- درس‌های گرفته‌شده از اشتباهات (M-numbered در بخش ۱۸)
- توضیحات معماری و تصمیمات
```

**افزودن به سند بخش ۱.۹ و سند ۱۷ (Templates)**

**ادغام در:** v2.11 (important)  
**ثبت‌شده توسط:** قانون #۶۰

---

### آیتم UX3.1 — ثبت درس از اشتباهات در سند (تفاوت با UX2.1)

**کشف:** چت ۸، پایان چت (درخواست کاربر) — 2026-05-19  
**سطح:** 🎯 important / UX  
**درخواست کاربر:** «درسهایی که از اشتباهاتت میگیری و قراره در سند درج کنی هم بیار»

**تفاوت با UX2.1:**
- **UX2.1 (پنهان):** جزئیات تشخیص خطا + روش اصلاح تدریجی
- **UX3.1 (نمایش):** درس کلی برای ثبت در M-numbered

**راه‌حل پیشنهادی:**

افزودن قانون جدید (پس از #۶۴):

```
**#۶۵** ⭐ v2.11 — **ثبت درس از اشتباهات در سند با نمایش به کاربر** —
وقتی Claude اشتباهی می‌کند که منجر به ثبت درس عمومی می‌شود
(M-numbered در بخش ۱۸ سند جامع)، باید:
(ا) درس را در PENDING_FOR_NEXT_VERSION.md ثبت کند (برای ادغام بعدی)
(ب) **درس کلی را به کاربر نشان دهد** (نه جزئیات تشخیص)
(ج) فرمت نمایش: "📌 درس جدید (M{N}): [درس کلی در یک جمله]"

تفاوت با #۶۴:
- #۶۴: چرا خطا داد + تشخیص چطور بود → پنهان
- #۶۵: درس کلی که را در سند ثبت می‌کنیم → نمایش
```

**افزودن به سند بخش ۱.۹ و سند ۱۸ مقدمه**

**ادغام در:** v2.11 (important)  
**ثبت‌شده توسط:** قانون #۶۰

---

## 📌 پایان فایل

**نسخه:** v0.1 (2026-05-19 — تولد در چت ۸، اولین فایل از این نوع)  
**ساخته توسط:** Claude در چت ۸ (`TRADING-phase0-part08-pre-phase1-setup`)  
**به‌روز توسط:** Claude در هر چت که PENDING کشف کند (قانون #۶۰)  
**ادغام بعدی:** سند جامع v2.11 (در چت ۹ یا بعد)

## 📊 خلاصه آیتم های PENDING برای v2.11

| آیتم | سطح | توضیح |
|---|---|---|
| **B5.1** | 💡 minor | فرمت `.gitignore` برای `claude_workspace/` (پیشنهاد سند ۲۴.۳ vs فعلی) |
| **B2.1** | 🎯 important | اصلاح مسیر Memory toggles در سند ۲۳.۱ (Capabilities نه Profile) |
| **C1.1** | 📊 medium | نتایج Settings audit — افزودن به سند ۲۳.۴ |
| **D1.1** | 🔴 important | Bug pre-commit `end-of-file-fixer` با فایل فارسی Windows |
| **E2.1** | 🟡 medium | Bug #50 cleanup — `jsxRuntime: 'automatic'` + حذف `import React` |
| **D2.1+D3.1** | 🟢 low | ارزیابی Claude Code + GitHub MCP (موکول به فاز ۴+) |
| **G5.1** | 🔴 important | قانون #۶۲ — فایل handoff دائمی در incoming_permanent |
| **UX1.1** | 🎯 important | قانون #۶۳ — Convention `🟢 ▶️ EXECUTE` با تیتر سبز |
| **UX2.1** | 🎯 important | قانون #۶۴ — عدم نمایش جزئیات تصحیح خطای کد |
| **UX3.1** | 🎯 important | قانون #۶۵ — ثبت درس از اشتباهات با نمایش به کاربر |

**تعداد:** ۱۰ آیتم  
**اولویت ادغام:** D1.1، B2.1، G5.1، UX1.1، UX2.1، UX3.1 در v2.11 اول → بقیه به ترتیب
