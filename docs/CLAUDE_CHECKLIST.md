# ✅ CLAUDE_CHECKLIST — چک‌لیست اجباری عملیاتی

> **هدف یک‌خطی:** فهرست explicit و قابل اجرای task ها برای Claude در ابتدا، میانه و انتهای هر چت.

> **محل قرارگیری:** `docs/CLAUDE_CHECKLIST.md`  
> **مرجع بالاتر:** `PROJECT_GOVERNANCE.md` و سند جامع v2.10+  
> **نسخه:** v1.3 (2026-05-19 — Atomic Update چت ۸، +۱۴ قانون #۴۸-۶۱ + MCP integration)

---

## 📑 فهرست

- [اصول](#اصول)
- [فاز ۱ — چک‌لیست شروع چت (۸ مرحله)](#فاز-۱--چکلیست-شروع-چت-۸-مرحله)
- [فاز ۲ — چک‌لیست در حین کار (per-task)](#فاز-۲--چکلیست-در-حین-کار-per-task)
- [فاز ۳ — چک‌لیست پایان چت (۱۲ مرحله)](#فاز-۳--چکلیست-پایان-چت-۱۲-مرحله)
- [Sub-checklists تخصصی](#sub-checklists-تخصصی)
- [مثال‌های اجرای موفق و ناموفق](#مثالهای-اجرای-موفق-و-ناموفق)
- [کلمات کلیدی Trigger](#کلمات-کلیدی-trigger)

---

## اصول

1. این چک‌لیست **اجباری** است، نه پیشنهادی.
2. هر مرحله باید **explicitly** انجام و در ذهن Claude (نه فقط implicit) چک شود.
3. اگر مرحله‌ای **نمی‌توان** انجام داد، Claude **توقف می‌کند** و گزارش می‌دهد.
4. ترتیب مراحل **متوالی** است. پرش به جلو ممنوع.
5. این چک‌لیست در **PROJECT_GOVERNANCE.md بخش ۴ و ۵** ارجاع داده شده.

---

## فاز ۱ — چک‌لیست شروع چت (۸ مرحله)

> ⏱️ **زمان کلی:** ۳-۵ دقیقه  
> 🎯 **هدف:** Claude باید دقیقاً بداند پروژه کجاست و چه باید کرد، قبل از هر اقدام.

### ✅ مرحله ۱ — بازشناسی پیوست‌ها

```
چک‌لیست:
[ ] آیا فایل zip پیوست است؟
[ ] آیا سند جامع v2.X پیوست است؟
[ ] آیا کاربر دستور خاصی داد یا فقط زمینه‌سازی کرد؟
[ ] آیا فایل دیگری پیوست است (مثل screenshot، خطا)؟
```

**خروجی این مرحله:** لیست فایل‌های در دسترس + درک اولیه از چت.

**اگر هر کدام غایب:** اعلام به کاربر، توقف.

---

### ✅ مرحله ۲ — استخراج و بررسی ساختار zip

```bash
# bash_tool یا view
unzip -q trading-system.zip -d /home/claude/
view /home/claude/trading-system
```

**چک‌لیست:**
```
[ ] فولدر docs/ موجود؟
[ ] فولدر backend/ موجود؟
[ ] فولدر frontend/ موجود؟
[ ] فولدر scripts/ موجود؟
[ ] لیست اسکریپت‌ها را دیدم؟ (شماره آخر چند است؟)
```

---

### ✅ مرحله ۳ — خواندن اسناد به ترتیب الزامی

ترتیب **حساس** است:

```
۱. docs/PROJECT_GOVERNANCE.md   (راهبردی — کامل)
۲. docs/PROJECT_CONTEXT.md       (سراسری — کامل)
۳. docs/SESSION_STATUS.md        (وضعیت آخرین — کامل)
۴. docs/CHAT_LOG.md              (تاریخ — کامل)
۵. docs/TASK_BACKLOG.md          (TODO ها — کامل)
۶. docs/CLAUDE_CHECKLIST.md      (همین سند — برای یادآوری)
۷. **docs/PENDING_FOR_NEXT_VERSION.md** 🆕 v2.10 (PENDING-EOC از چت قبل — قانون #۴۸+#۶۰)
۸. سند جامع v2.X                (کامل، شامل **بخش ۱۸ درس‌نامه M1-M62** — قانون #۴۸)
```

**اگر سندی غایب:**
- اگر **اختیاری** بود (مثل GLOSSARY): ادامه با گزارش به کاربر
- اگر **اجباری** بود (مثل سند جامع، CHAT_LOG): **توقف**

---

### ✅ مرحله ۴ — بررسی محیط (Post-unzip)

```bash
# چک ۳ مورد بازسازی‌پذیر
[ ] backend/venv/ موجود؟
[ ] backend/trading.db موجود؟
[ ] frontend/node_modules/ موجود؟
```

**خروجی:**
- اگر **همه موجود:** ادامه به مرحله ۵
- اگر **یک یا بیشتر غایب:**
  - پیشنهاد اجرای `scripts/00b_post_unzip_setup.py`
  - **هرگز فرض نکن "نیاز نیست چون کاربر قبلاً آمده"** — صریح بپرس

---

### ✅ مرحله ۵ — درک Bug ها و تصمیمات گذشته

**از اسناد، استخراج کن:**

```
[ ] آخرین Bug ها چه بودند؟ (TROUBLESHOOTING.md)
[ ] آخرین تصمیمات معماری چه بودند؟ (DECISIONS_LOG.md)
[ ] چه قوانین جدیدی در چت‌های اخیر اضافه شده؟ (سند جامع تغییرات v2.X)
[ ] چه Anti-Pattern هایی الان حساس هستند؟ (PROJECT_GOVERNANCE بخش ۱۱)
```

---

### ✅ مرحله ۶ — تعیین Tier فعلی و گام‌های ممکن

**از TASK_BACKLOG.md:**
- Tier فعلی چیست؟ (1/2/3/4)
- Tasks باز در این Tier کدامند؟
- آیا کاربر یک task مشخص خواست یا گزینه می‌خواهد؟

---

### ✅ مرحله ۷ — تولید گزارش آمادگی

Claude **یک پیام واحد** ارسال می‌کند با این ساختار:

```markdown
سلام! بررسی فایل‌ها انجام شد.

## 📍 وضعیت فعلی پروژه

- **فاز:** [N]
- **پیشرفت:** [%]
- **آخرین چت:** [نام چت قبلی از CHAT_LOG]
- **آخرین کار انجام‌شده:** [خلاصه]

## 🔍 محیط

| چک | وضعیت |
|---|---|
| backend/venv | ✅ / ❌ |
| backend/trading.db | ✅ / ❌ |
| frontend/node_modules | ✅ / ❌ |

[اگر هرکدام ❌: پیشنهاد 00b]

## 📚 خوانده‌شده

- [x] PROJECT_GOVERNANCE
- [x] PROJECT_CONTEXT
- [x] SESSION_STATUS
- [x] CHAT_LOG
- [x] TASK_BACKLOG
- [x] سند جامع v2.X

## 📋 Tasks باز در Tier فعلی (Tier {N})

- [ ] Task A
- [ ] Task B
- ...

## 🚀 گزینه‌های گام بعدی

الف) [...]
ب) [...]

[اگر کاربر دستور خاص داد، آن را به‌عنوان "گزینه اولیه" قرار بده ولی جایگزین نکن]

منتظر انتخاب شما هستم.
```

---

### ✅ مرحله ۸ — صبر برای تأیید کاربر

**Claude حق آغاز کار را ندارد** تا یکی از این‌ها اتفاق افتد:
- کاربر گزینه‌ای را انتخاب کند
- کاربر دستور صریح بدهد
- کاربر تأیید کند ("بریم")

**ممنوع:** پرش به اجرای کد، بدون تأیید.

---

## فاز ۲ — چک‌لیست در حین کار (per-task)

این چک‌لیست **در هر گام کاری** اجرا می‌شود.

### قبل از تولید اسکریپت

```
[ ] آیا قبل از نوشتن، فایل‌های فعلی را خواندم؟
[ ] آیا قانون #۲۲ را به یاد دارم؟ (نیاز به اسکریپت تست همراه)
[ ] آیا اسکریپت idempotent خواهد بود؟
[ ] آیا Anti-Pattern های A1-A10 را در نظر دارم؟
```

### حین نوشتن کد (frontend)

```
[ ] هیچ hex hardcoded ندارم؟
[ ] از CSS variables استفاده می‌کنم؟
[ ] اگر variant: کانتینر از تم + accent باریک (سند ۸.۸.۱)
[ ] Interactive states (hover/active/focus-visible/disabled) رعایت شد؟
[ ] از rem نه px برای fontSize استفاده می‌کنم؟
[ ] ARIA attributes برای accessibility دارم؟
```

### حین نوشتن کد (backend)

```
[ ] Query از Repository می‌رود نه مستقیم از DB؟
[ ] هیچ hardcode در .env-able value ندارم؟
[ ] # -*- coding: utf-8 -*- در ابتدای فایل Python؟
[ ] Exception handling مناسب؟
[ ] Logger به جای print؟
```

### حین استفاده از Filesystem MCP (🆕 v2.10)

```
[ ] قبل از write: ساختار preview شده؟ (M60)
[ ] قبل از write: تأیید صریح کاربر گرفته شد؟ (قانون #۵۱)
[ ] بعد از write: read-back با read_text_file انجام شد؟ (قانون #۳۷، M61)
[ ] مسیر تحویل صریح اعلام شد؟ (قانون #۵۹)
[ ] هر [PENDING-EOC] کشف‌شده در PENDING_FOR_NEXT_VERSION.md ثبت شد؟ (قانون #۶۰)
[ ] خود-اجرایی قوانین خود-ذکر شده (M62) رعایت شد؟
```

### قبل از تحویل اسکریپت

```
[ ] اجرا کردم محلی (در محیط Claude)؟
[ ] تست idempotency (اجرای دوم بدون خطا)؟
[ ] تست خودکار pass شد؟
[ ] خروجی مثال انتظاری دارم؟
[ ] دستور اجرا با نام tab صحیح (🟦/🟩/🟧)؟
```

---

## فاز ۳ — چک‌لیست پایان چت (۱۲ مرحله)

> ⏱️ **زمان کلی:** ۲۰-۴۰ دقیقه  
> 🎯 **هدف:** هیچ‌چیز از این چت گم نشود.

### ✅ مرحله ۱ — جمع‌بندی کارهای انجام‌شده

```
[ ] لیست اسکریپت‌های جدید (Nb_test_ همراه چک شد؟)
[ ] لیست Bug fix ها
[ ] لیست تصمیمات معماری
[ ] لیست feature ها
[ ] لیست TASK های DONE شده
[ ] لیست TASK های ناتمام (برای چت بعد)
```

### ✅ مرحله ۲ — به‌روزرسانی CHAT_LOG.md

```
[ ] افزودن بخش جدید با نام چت
[ ] تاریخ، اسکریپت‌ها، فایل‌ها، Bug ها، تصمیمات
[ ] نکات برای برنامه‌نویس آینده
```

### ✅ مرحله ۳ — به‌روزرسانی TASK_BACKLOG.md

```
[ ] task های DONE → بخش DONE History با timestamp
[ ] task های جدید کشف‌شده → بخش مناسب (Tier 1/2/3/4)
[ ] task های ناتمام → بخش IN-PROGRESS
```

### ✅ مرحله ۴ — به‌روزرسانی DECISIONS_LOG.md

اگر تصمیم معماری گرفته شد:
```
[ ] افزودن Decision #N با:
  - عنوان
  - چه کسی پیشنهاد داد
  - چه گزینه‌هایی بررسی شد
  - چرا این انتخاب شد (trade-offs)
  - پیامدها
```

### ✅ مرحله ۵ — به‌روزرسانی TROUBLESHOOTING.md

اگر Bug جدید شناسایی و رفع شد:
```
[ ] افزودن Bug #N با:
  - علائم
  - علت ریشه‌ای
  - راه‌حل
  - نحوه جلوگیری در آینده
```

### ✅ مرحله ۶ — به‌روزرسانی GLOSSARY.md

اگر اصطلاح جدید معرفی شد:
```
[ ] افزودن واژه + تعریف + لینک به سند مرجع
```

### ✅ مرحله ۷ — به‌روزرسانی STYLE_GUIDE.md

اگر pattern جدید معرفی شد:
```
[ ] افزودن pattern با مثال before/after
```

### ✅ مرحله ۸ — به‌روزرسانی SESSION_STATUS.md

```
[ ] نام چت فعلی
[ ] خلاصه ۳-۵ خطی
[ ] فاز جاری + پیشرفت %
[ ] گام بعدی (پیشنهادی)
[ ] نام پیشنهادی چت بعد
```

### ✅ مرحله ۹ — به‌روزرسانی PROJECT_CONTEXT.md

```
[ ] مراجع به اسناد جدید (اگر اضافه شدند)
[ ] خلاصه stack updates (اگر چیزی نصب/حذف شد)
[ ] قوانین جدید (اگر اضافه شدند)
```

### ✅ مرحله ۱۰ — به‌روزرسانی CHANGELOG.md

اگر version bump:
```
[ ] افزودن بخش [vX.Y.Z] — {تاریخ}
[ ] زیربخش‌ها: Added, Changed, Fixed, Decided, Pending
```

### ✅ مرحله ۱۱ — به‌روزرسانی سند جامع

**مهم‌ترین:**
```
[ ] افزایش minor version (v2.X → v2.X+1)
[ ] افزودن بخش "تغییرات این نسخه" در ابتدا
[ ] افزودن Bug های جدید به بخش Bug history
[ ] افزودن Decision های جدید به بخش Decisions
[ ] افزودن قوانین جدید به بخش قوانین
[ ] هیچ بخشی حذف نشد (No-Deletion principle)
[ ] منسوخ‌ها با علامت [منسوخ — vX.Y]
```

### ✅ مرحله ۱۱.۵ — پاک‌سازی PENDING_FOR_NEXT_VERSION.md (🆕 v2.10)

پس از ادغام موفق PENDING → سند جامع v(X+1):

```
[ ] محتوای بخش‌های ۱-۶ فایل پاک شد
[ ] هدر + بخش ۰ + بخش ۷ + بخش تاریخچه ادغام حفظ شد
[ ] خط جدید در «تاریخچه ادغام‌ها» اضافه شد: `آخرین ادغام: v2.X → v2.X+1 در چت N (تاریخ Y) — تعداد موارد: Z`
[ ] فایل را never delete نکن — همیشه draft زنده باقی می‌ماند
```

این طبق پروتکل بخش ۷.۳ خود فایل PENDING انجام می‌شود (مرجع: قانون #۶۰).

### ✅ مرحله ۱۲ — ساخت zip نهایی + پیام پایانی

```
[ ] zip شامل همه ۱۲ سند به‌روز
[ ] zip شامل scripts/ (بدون __pycache__)
[ ] zip شامل کد به‌روز
[ ] backend/venv/ → exclude
[ ] frontend/node_modules/ → exclude
[ ] backend/trading.db → exclude (بازسازی توسط 00b)
[ ] backend/logs/ → exclude
[ ] پیام پایانی شامل:
  - نام چت فعلی
  - نام چت بعدی پیشنهادی
  - فهرست تغییرات
  - لینک download زip
```

---

## Sub-checklists تخصصی

### چک‌لیست تولید اسکریپت Python

```
[ ] # -*- coding: utf-8 -*-
[ ] Path-based (نه string concat)
[ ] write_if_changed pattern برای idempotency
[ ] try/except با گزارش معنادار
[ ] خروجی stdout گویا
[ ] اسکریپت تست Nb همراه
```

### چک‌لیست تولید کامپوننت React

```
[ ] export default function
[ ] props با destructuring
[ ] هیچ hex (همه var(--...))
[ ] ARIA attributes
[ ] interactive states (hover/focus/active/disabled)
[ ] fontSize با rem
[ ] cleanup در useEffect (return function)
```

### چک‌لیست افزودن endpoint جدید

```
[ ] route در app/api/v1/routes/
[ ] dependency injection (DB، current_user)
[ ] response با ساختار استاندارد {data, status, message}
[ ] HTTP code صحیح
[ ] OpenAPI tags + summary
[ ] اسکریپت تست با httpx
[ ] افزودن به PROJECT_CONTEXT.md جدول API
```

### چک‌لیست افزودن Model

```
[ ] در app/models/
[ ] inherit از Base
[ ] __tablename__ صحیح
[ ] indexes ضروری
[ ] migration با Alembic
[ ] Repository در app/repositories/
[ ] افزودن به سند جامع بخش ۵
```

---

## مثال‌های اجرای موفق و ناموفق

### ✅ مثال موفق: شروع چت

```
User: [پیام شروع چت با zip + سند v2.7]

Claude: [اجرای ۸ مرحله فاز ۱]
سلام! بررسی فایل‌ها انجام شد.

## 📍 وضعیت
- فاز: ۰
- پیشرفت: ۱۰۰٪
- آخرین چت: TRADING-phase0-part05-...
- آخرین کار: اتمام فاز ۰ + ساخت Governance

## 🔍 محیط
| backend/venv | ✅ |
| trading.db | ✅ |
| node_modules | ✅ |

## 📋 Tasks باز (Tier 2)
- [ ] Error Boundaries
- [ ] Frontend tests
- ...

## 🚀 گزینه‌ها:
الف) شروع Tier 2 ...
ب) ...

منتظر انتخاب شما هستم.
```

### ❌ مثال ناموفق: شروع چت

```
User: [پیام شروع چت]

Claude: سلام! بریم سراغ Error Boundaries؟
```

**چرا ناموفق؟**
- ندید اسناد را نخوانده
- فرض کرد کاربر می‌خواهد Tier 2
- نپرسید
- چک‌لیست را اجرا نکرد

---


## قوانین رفتاری Claude — یادداشت سریع (طبق سند جامع v2.8 جدول ۱.۹)

این جدول، خلاصه قوانین که در طول هر چت باید رعایت شوند. مرجع کامل: سند جامع `سند_جامع_v2_8.md` بخش ۱.۹.

| # | یادآور |
|---|---|
| #۱۴ | تولید فایل با Artifact (نه Notepad دستی) |
| #۱۷ + #۳۱ | بالای هر کادر کد: 🟦/🟩/🟧/🟥 + شماره tab |
| #۱۸ | لینک فایل دانلودی بالای بلوک دستور |
| #۲۰ | پایان چت → ساخت zip خودکار |
| #۲۲ | هر اسکریپت `{N}_*.py` → اسکریپت `{N}b_test_*.py` همراه |
| #۲۳ | پایان هر چت → بخش جدید در CHAT_LOG.md |
| #۲۴ | سند جامع: No-Deletion |
| #۲۶ | تغییرات → Atomic در همه اسناد مرتبط |
| **#۲۷ (v2.8)** | **هیچ‌گاه پایان چت خودکار — منتظر تأیید صریح کاربر** |
| **#۲۸ (v2.8)** | **خطا → فقط اقدام اجرایی، بدون توضیح فنی طولانی** |
| **#۲۹ (v2.8)** | **فایل = Artifact یا code block (هرگز paste دستی)** |
| **#۳۰ (v2.8)** | **اصلاحات کوچک فایل = اسکریپت Python idempotent** |
| **#۳۱ (v2.8)** | **شماره tab + رنگ tab بالای هر کادر کد** |
| **#۳۲ (v2.8)** | **npm install در ایران: registry mirror چینی + flag بهینه** |
| **#۴۸ (v2.10)** | **شروع چت → خواندن بخش ۱۸ + PENDING_FOR_NEXT_VERSION.md اجباری** |
| **#۴۹ (v2.10)** | **MCP read-only: Always Allow؛ write: Needs Approval؛ هرگز .env** |
| **#۵۱ (v2.10)** | **تأیید صریح قبل از هر MCP write/delete** |
| **#۵۶ (v2.10)** | **مسیر تحویل فایل صریح اعلام شود** |
| **#۵۹ (v2.10)** ⭐ | **بلااستثنا اعلام مسیر دانلود/قرارگیری هر فایل** |
| **#۶۰ (v2.10)** ⭐⭐⭐ | **PENDING-EOC در لحظه با MCP در docs/PENDING_FOR_NEXT_VERSION.md** |
| **#۶۱ (v2.10)** ⭐ | **در سؤالات چندگزینه‌ای، پیشنهاد مطلوب خود را صریح بگو** |

## کلمات کلیدی Trigger

اگر کاربر این کلمات را گفت، Claude **فوری** پاسخ مناسب می‌دهد:

| کلمه | معنی | اقدام Claude |
|---|---|---|
| **Governance** | "Governance رو رعایت کن" | بازخوانی PROJECT_GOVERNANCE + اعمال |
| **چک‌لیست** | "چک‌لیست رو دیدی؟" | اعلام کدام مراحل انجام شد |
| **E1: کیفیت** | افت کیفیت تشخیص | شروع Protocol پایان چت فوری |
| **پایان چت** | درخواست بسته‌شدن | شروع فاز ۳ (فقط با تأیید صریح — قانون #۲۷) |
| **توقف** | stop | فوری توقف، انتظار دستور |
| **بازگشت به سند** | الان دارم منحرف می‌شم | بازخوانی سند مرتبط |
| **چت ببند** | تأییدیه صریح پایان چت (قانون #۲۷) | شروع فاز ۳ |
| **end of chat** | تأییدیه صریح انگلیسی پایان چت (قانون #۲۷) | شروع فاز ۳ |

---

## قوانین قفل‌شده مرتبط با چک‌لیست (مرجع سریع)

این جدول قوانینی را که به‌طور خاص به اجرای این چک‌لیست مربوط هستند فهرست می‌کند. **مرجع کامل قوانین:** جدول ۱.۹ در سند جامع v2.8.

| # | قانون | کجای چک‌لیست اعمال می‌شود |
|---|---|---|
| #۲۵ | چک‌لیست اجباری شروع چت (۸ مرحله) | فاز ۱ — کامل |
| #۲۷ | تأیید صریح کاربر برای پایان چت | فاز ۳ — قبل از مرحله ۱ |
| #۲۸ | کوتاه گفتن خطا | فاز ۲ — حین کار |
| #۲۹ | ارائه فایل با Artifact یا code block | فاز ۲ — قبل از تحویل |
| #۳۰ | اصلاحات کوچک = اسکریپت Python idempotent | فاز ۲ — قبل از تولید اسکریپت |
| #۳۱ | شماره + رنگ tab بالای هر کادر کد | فاز ۲ — قبل از تحویل |
| #۳۲ | npm install در ایران: registry.npmmirror.com + flags | چک محیط (مرحله ۴ فاز ۱) |
| #۳۳ | Backup فقط در پایان چت | پیش از commit -- نه نیاز |
| #۳۴ | zip در root پروژه دانلود | کاربر زیپ ها را در root می‌گذارد |
| #۳۵ | pip flags ≠ npm flags | فقط آن flag ها که برای ابزار مربوطه |
| #۳۶ | verify signature قبل از تست | findstr پیش از نوشتن تست |
| #۳۷ | read-back verify بعد از write | پس از تولید/تغییر فایل |
| #۳۸ | `.py` -> scripts/، zip -> root | مسیر کپی صریح |
| #۳۹ | multi-root zip -> `python -m zipfile -e` | به‌جای 43_sync_from_zip |
| #۴۰ | verify argparse syntax | پیش از پیشنهاد command |
| #۴۱ | `.get()` به‌جای `[]` در `or` | در assertion تست |
| #۴۲ | `--no-verify` با `[skip-hooks: REASON]` | فقط در اضطرار |
| #۴۳ | Hybrid hook mode | critical اجباری، minor warning |
| #۴۴ | `.gitattributes` نه hook برای CRLF | line ending control |
| #۴۵ | pre-commit entry: `python wrapper.py` | نه .cmd مستقیم |
| #۴۶ | ASCII-only در print() اسکریپت Windows | + utf-8 reconfigure |
| #۴۷ | تست hook قبل از deploy | روی کل codebase |
| #۴۸ | پروتکل اجباری شروع چت — بخش ۱۸ + PENDING | فاز ۱ — مرحله ۳ |
| #۴۹ | Filesystem MCP permissions | چک محیط (مرحله ۴ فاز ۱) |
| #۵۱ | تأیید صریح قبل از MCP write/delete | فاز ۲ — Sub-checklist MCP |
| #۵۶ | اعلام مسیر تحویل فایل | فاز ۲ — قبل از تحویل |
| #۵۹ | بلااستثنا اعلام مسیر دانلود | فاز ۲ — قبل از تحویل |
| #۶۰ | PENDING-EOC در لحظه ثبت | فاز ۲ — Sub-checklist MCP + فاز ۳ مرحله ۱۱.۵ |
| #۶۱ | پیشنهاد گزینه مطلوب در چندگزینه‌ای | فاز ۲ — حین کار |

---

## ⛔ پیش‌شرط بحرانی فاز ۳ — قانون #۲۷

**قبل از شروع هر یک از ۱۲ مرحله فاز ۳ (پایان چت)، Claude باید تأیید صریح کاربر را دریافت کرده باشد.**

### تأییدیه‌های صریح که فاز ۳ را trigger می‌کنند:
- «چت رو ببند»
- «end of chat»
- «zip نهایی بساز و چت رو ببند»
- «پایان چت»
- «بسته کن این چت رو»

### جملاتی که تأییدیه نیستند (نباید فاز ۳ را trigger کنند):
- «گام بعدی چیست؟»
- «کار دیگری هست؟»
- «چی مونده؟»
- «خسته شدیم»

### استثنا
نزدیک‌شدن به سقف context window — با هشدار قبلی و انتظار تأیید کاربر برای ادامه به‌صورت پایان چت.

### رفتار صحیح بعد از اتمام task
1. Claude خلاصه آنچه انجام داد را ارائه می‌دهد
2. Claude پیشنهاد گام بعدی می‌دهد (مثلاً «task بعدی T2.06 است — شروع کنم؟»)
3. Claude **منتظر دستور بعدی می‌ماند** — نه به فاز ۳ می‌رود نه به task بعدی

---

## 🎨 رعایت قانون #۳۱ — شماره + رنگ tab

**بالای هر کادر کد دستوری، Claude باید این کار را انجام دهد:**

````markdown
**🟦 tab «1 backend»**
```cmd
cd backend
venv\Scripts\activate
pytest
```
````

### رنگ‌بندی پیش‌فرض ۴ tab

| Tab | رنگ | کاربرد | پوشه فعال پیش‌فرض |
|---|---|---|---|
| `1 backend` | 🟦 آبی | سرور + تست backend + venv | `backend/` |
| `2 scripts` | 🟩 سبز | اسکریپت‌های Python | پروژه root |
| `3 frontend` | 🟧 نارنجی | React + تست frontend | `frontend/` |
| `4 BACKUP` | 🟥 قرمز | git + backup + sync | پروژه root |

### استثنا
کادرهای کد non-executable (مثل کد JSX برای نشان دادن، YAML برای توضیح، JSON نمونه) نیاز به برچسب tab ندارند.

---

## 📌 پایان CLAUDE_CHECKLIST

**نسخه:** v1.4 (2026-05-20 — Atomic Update چت ۹: +۴ قانون UX #۶۲-۶۵ + M63-M64 + Bug #50 cleanup)  
**مرجع بالاتر:** PROJECT_GOVERNANCE.md + سند جامع v2.11+

### تغییرات v1.3 → v1.4

- **۴ قانون UX جدید (#۶۲-۶۵):**
  - #۶۲: فایل handoff دائمی در `claude_workspace/incoming_permanent/CHAT{N+1}_HANDOFF.txt` با prefix ‌های ✅/📋/⚠️/💡
  - #۶۳: Convention بصری برای گام‌های اجرایی: `## 🟢 ▶️ EXECUTE — اقدام لازم`
  - #۶۴: عدم نمایش جزئیات تصحیح خطای کد (پنهان: چرا/چطور؛ نمایش: گام اصلاحی)
  - #۶۵: ثبت درس از اشتباهات با نمایش به کاربر (📌 درس جدید M{N}: ...)
- **۲ درس جدید:** M63 (فرض نکردن status در handoff)، M64 (JSX runtime در plugin-react و esbuild جداگانه)
