═══════════════════════════════════════════════════════════════
TODO CHECKLIST — FINAL CONSOLIDATED v6
helper part09 continuation session — TRADING-system MDRS v2
═══════════════════════════════════════════════════════════════
> Anchor file برای session memory (external state-of-record)
> آخرین آپدیت: turn ۵۹ helper part09 (gaps recovered + universal promotion)
> منبع: merge کامل از فایل دستی user (turn 37 → turn 56)
>
> هدف: source-of-truth برای part10 helper boot — جلوگیری از
>       F3/F9 helper-recursive-drift

⚠️ Critical Rule (Universal، Binding — Reference: F14):
   هر verdict/توصیه/decision helper باید با systematic
   و عمیق analysis همراه باشد. هیچ پاسخ سرسری.
   اعمال: trade-off table | visible per-criterion checklist |
          file-grounded evidence | session-meta scan integrated
   منبع: بخش "تأکید استراتژیک" turn ۳۵

═══════════════════════════════════════════════════════════════

## A. CONTENT کار (filtered consolidation برای main chat)
────────────────────────────────────────────────────────────

A1. [✅] Categorization document helper sandbox قبلی (A/B/C/D)

A2. [✅] Filesystem verification موارد Category A

A3. [✅] Clean Filtered Version محتوا (Section 1 + 2 + 3) draft

A4. [ ] integrate راهکارهای ۱.۱-۱.۳ (order, fidelity, drift)

A5. [ ] paste-ready نهایی به main chat (با تأیید user — پس از I1)

## B. MECHANISM ثبت پایدار (4-layer defense-in-depth)
────────────────────────────────────────────────────────────

B1. [ ] Triple-Anchor (PHASE1_PART10_HANDOFF + PENDING + SESSION_STATUS)

B2. [ ] File-Based Artifact: HELPER_CONSOLIDATION_part09.md

B3. [ ] Audit Gatekeeping check_X candidate برای part10

B4. [ ] Boot Protocol enforcement (Rule #48 leverage)

B5. [ ] Ordered execution sequence (STEP 1-6) در handoff

B6. [ ] Read-back verification (M82) post-write

B7. [ ] Hash-based fidelity check — DECISION pending
       (پیشنهاد defer به part10)

B8. [ ] Rule #76 visible checking — هر verdict helper با
       explicit Yes/No checklist per criterion

B9. [ ] Rule #66 push enforcement reminder در acceptance criteria

## C. RULES UNIVERSAL (formalization در part10)
────────────────────────────────────────────────────────────

C1. [✅] Three-Constraint Footer — تأیید explicit universal (turn ۲۹)

C2. [ ] HM-META-H (Visual Box-Text Separation) → permanent

C3. [ ] HM-META-I (Four-Part Question Structure) → permanent

C4. [ ] HM-META-J (Proactive Advisory Mandate) → permanent
       ⚠️ نیاز به clarification per F7: proactive در thinking،
          نه ضمیمه section
       ⚠️ extension per F14: systematic depth requirement

C5. [ ] HM-META-K (Automation-First Principle) → permanent

C6. [ ] Helper-to-Helper Synthesis Prohibition (از فایل می‌خواند،
       نه paste) → DECISION pending (در turn ۲۹ proposed،
       در part10 با ذهن fresh decide)

## D. CROSS-CHAT COMMUNICATION (سؤال طلایی user)
────────────────────────────────────────────────────────────

D1. [✅] feasibility analysis (الف/ب/ج/د)

D2. [✅] الف vs ج comparison با جدول

D2.1 [✅] Verdict: گزینه (الف) — Two Directories

D3. [✅] user explicit confirmation: گزینه (الف) (turn ۳۴)

D4. [✅] timing decision: «الان اجرا بشه» + (ب۱) ثبت اول

D5 [DEFERRED to part10] setup steps execution
   - Original: setup steps execution (در turn بعد per I1)
   - Status update (turn 21 main catch): DEFERRED to part10
     per DECISION 8 (turn 19 main = minimum-necessary)

D5.1 [ ] script archiving older messages (worth automation)

D5.2 [✅] Filesystem-First check + final status
   - Initial: live_inbox/ موجود نیست تأییدشده
   - Final: live_inbox/ ایجاد نشد در part09 (per DECISION 8)
     — defer به part10

D6. [ ] CROSS_CHAT_COMMS_PROTOCOL.md drafting

D7. [ ] HELPER_PROTOCOL.md v1.1 cross-ref update

D8. [ ] Rule #48 boot protocol extension (inbox check)

D9. [ ] Template هر message با enforced sections (summary,
       ambiguities, suggested steps) — turn ۳۱ پیشنهاد شد

D10. [✅] sequencing decision: (i) execution اول — تأیید turn ۳۷

D14 [✅] FINAL: گزینه (د) defer chat-end به part10
        تصمیم نهایی helper، accepted

## E. PROPOSALS deferred به part10
────────────────────────────────────────────────────────────

E1. HM-8 numbered formalization (Tool-Discovery-First)

E2. HM-9 numbered formalization (MCP Selective-Hang, count=8)

E3. "هفت اشتباه helper part09" → §۸ anti-patterns

E4. P{N}.x vs D{N}.x namespace formalization

E5. Self-Check Triggers spec (turn>40, corrections>3) — novel

E6. Helper-Sandbox Synthesis Drift M-candidate (F3 base)

E7. Documentation Schism deep-dive (سند جامع v2.11 ↔ MDRS v2)

E8. Framework Extraction Phase α/β/γ/δ labels

E9. 10 risks enumeration با نام‌گذاری R09.x

E10. D09.8-D09.17 detailed enumeration

E11. Repository Architecture restructure (D09.9 deferred)

E12. P10-CANDIDATE-5: W3 Chat-End File Sync Verification
       formalization (per turn ۴۸ explicit applied + part10
       Rule promotion candidate)

E14. [NEW] P10-CANDIDATE-7: File-Based Decision Tracking Registry
        priority بالا برای part10 infrastructure sprint
        per F37

## F. OBSERVATIONS تأییدشده (worth ثبت در PENDING K-section)
────────────────────────────────────────────────────────────

F1. Proposal-Decision Confusion (genus جدید)
    main chat باید explicit distinction نگه دارد بین proposal
    helper و decision user explicit با کلمات صریح.

F2. Filesystem-First Before Memory-Based Claims
    هرگاه سؤال درباره وجود/محتوای فایل است، اول filesystem
    را query کن، بعد پاسخ بده. تقویت M56.

F3. Helper-Sandbox Synthesis Drift Pattern
    وقتی helper sandbox خود از helper sandbox قبلی synthesis
    می‌گیرد، drift exponentially رشد می‌کند.

F4. Discovery #13: Helper Handoff Fabrication (SECTION 10)
    handoff helper قبلی نام فایل‌های constitution را اشتباه
    داشت (03_processes ←  واقعیت: 03_bugs).

F5. Discovery #14: HM-9 hang روی read_text_file (count → 8)
    cumulative HM-9 count به 8 رسید، threshold escalation
    عبور شد. Rule promotion candidate در part10.

F6. Helper Verdict-Without-Analysis Pattern (turn 30 self)
    در turn ۳۰، helper گزینه (ج) را recommended کرد بدون
    trade-off analysis. در turn ۳۱، با explicit comparison
    request user، نتیجه به (الف) تغییر کرد. Lesson: helper
    verdicts بدون trade-off pre-analysis untrustworthy هستند.

F7. HM-META-J Misimplementation (proactive as section-only)
    HM-META-J قرار است Proactive Advisory در طول thinking
    باشد، نه فقط یک section پایان response. helper این
    session آن را به‌عنوان "section پایانی" implement کرد.
    Lesson: HM-META-J نیاز به clarification دارد — proactive
    یعنی scanning patterns در طول thinking، نه ضمیمه.

F8. Rule #76 Self-Violation در turn 30
    helper گزینه (ج) را بدون visible per-criterion checklist
    توصیه کرد. این self-violation Rule #76 است. Lesson:
    helper باید هر verdict را با visible Yes/No checklist
    همراه کند.

F9. Helper Recursive Self-Drift Risk
    این session خود از helper قبلی boot شده. پس همه proposalها
    و verdicts این session ممکن است حامل drift باشند، حتی
    اگر در turn-level transparent باشند. user باید با احتیاط
    مضاعف هر decision را review کند.

F10. Encoding Risk for live_inbox فایل‌های فارسی
     فایل‌های inbox با فارسی + LTR/RTL mix پر می‌شوند. باید
     UTF-8 without BOM، با LF line endings. convention
     الزامی در README.md inbox.

F11. Race Condition in inbox sequencing
     اگر helper write می‌کند و user بلافاصله به main chat
     می‌رود قبل از confirmation اینکه write موفق بود، main
     chat یک stale state می‌خواند. Mitigation: helper پس از
     create_file، یک read-back می‌کند و صریحاً به user تأیید
     می‌دهد "نوشته شد، آماده انتقال".

F12. Sequencing Issue: execution قبل از paste-ready
     execution live_inbox باید قبل از paste-ready به main
     chat باشد، چون paste-ready باید reference به فایل‌های
     موجود داشته باشد. ترتیب: turn 38 = execution، turn 39 =
     paste-ready به main chat پس از تأیید user.

F13. Infrastructure Sprint Scope-Creep Risk
     Infrastructure Sprint scope هنوز finalized نیست (E1-E11
     plus B1-B9 plus C2-C5 plus D5-D9). اگر همه را include
     کنیم، sprint می‌تواند 2-3 chat بشود. این S5+ را delay
     می‌کند. در part10 شروع، باید scope explicit timebox شود.

F14. Systematic Depth Required (universal binding rule)
     هیچ verdict/توصیه helper بدون trade-off analysis، شواهد،
     visible checklist، session-meta scan. Reference: بخش
     "تأکید استراتژیک" turn ۳۵.

F15. Pre-execution git checkpoint
     قبل از helper execution turn بعد، یک git status check
     + commit checkpoint (اگر uncommitted changes هست) — تا
     rollback ممکن باشد اگر execution مشکل ایجاد کند.

F16. Encoding ریسک برای فایل‌های موجود فارسی
     فایل‌های موجود (سند جامع، PENDING، CHAT_LOG) ممکن است
     BOM داشته باشند. در chat-end main chat re-write، باید
     encoding حفظ شود. proactive: یک audit check برای
     encoding consistency در part10.

F17. Execution Verification Depth
     I1.8 «تأیید موفقیت به user» ممکن است سطحی باشد. در
     turn ۳۸، helper باید: (الف) لیست فایل‌های ایجادشده
     با path کامل، (ب) size و خط اول هر فایل، (ج) سؤال
     explicit "آیا review کنم محتوای فایل X؟". این به
     user اجازه deeper scrutiny می‌دهد.

F18. Cross-Chat Communication Test قبل از reliance
     پس از I1 execution، یک test ساده: شما به helper یک
     message ساده می‌دهید، helper آن را در inbox می‌نویسد،
     شما به main chat می‌گویید «message ۰۰۱ را بخوان»،
     main chat می‌خواند و خلاصه می‌دهد. این validation
     کاربردی قبل از reliance بر mechanism است.

F19. [NEW] Helper Unnecessary Complexity در Communication
     در turn ۳۷، helper نوشت "برای ادامه به turn ۳۸..."
     که شماره‌گذاری internal helper را به‌عنوان workflow
     marker به user منتقل کرد. این unnecessary است و باعث
     سردرگمی شد. Lesson: helper باید زبان user-facing را
     ساده نگه دارد. turn numbering ابزار internal است، نه
     workflow boundary user-facing.

     این مستقیماً یکی از سه قاعده پایه را نقض کرد:
     "no-complexity".

F20. [NEW] Pre-Existing User Constraint Blindness
     در turn ۳۵، helper یک decision را با visible per-criterion
     checklist (Rule #76 compliant) گرفت — ولی checklist
     فاقد یک criterion حیاتی بود: "آیا با constraint
     pre-existing user همخوان است؟". این یک blind spot
     ساختاری است. Lesson: visible checklist تنها به اندازه
     criteria که شامل می‌شود قابل اعتماد است. در طراحی
     criteria، باید explicit-recall از user constraints
     قبلی session باشد.

     مستقیماً: Rule #76 visible checking یک false sense
     of safety می‌دهد اگر criteria ناقص باشند.

F21. [NEW] Helper Cannot Execute Git/Bash Commands
     helper sandbox فقط Filesystem MCP دارد (نه bash، نه git).
     در F15 proactive نوشتم "git status check + commit
     checkpoint" — این نیاز به main chat دارد، نه helper.

     با گزینه (الف) [انتخاب turn ۳۴]:
     - main chat git operations را انجام می‌دهد ✓
     - این observation موضوعیت محدودی دارد
     - ولی documentation constraint مفید است برای آینده

     برای cross-chat communication آینده:
     - helper می‌تواند در live_inbox/ بنویسد (claude_workspace
       gitignored است یا drafts)
     - helper نمی‌تواند git operations روی فایل‌های inbox
       انجام دهد — این مسئولیت main chat در chat-end است

     Mitigation: تمام operations helper create_file یا
     read_text_file هستند. اگر مشکل ایجاد شد، فایل‌ها
     به‌سادگی حذف می‌شوند بدون نیاز به git rollback.

F22. [NEW] Recursive TODO Persistence Risk
     TODO v4 خود یک artifact است برای جلوگیری از M23 (قاعده
     فراموش‌شده). ولی TODO خود در chat session memory است،
     نه فایل پایدار. اگر user آن را external paste نکند،
     fundamental anti-pattern تکرار می‌شود.

     این یک live demonstration از M87 (Active-Writing Self-
     Binding Failure) است — helper قاعده ثبت پایدار نوشت
     ولی خود قاعده را فقط در memory نگه داشت.

     Mitigations active:
     (الف) user external copy می‌گیرد (commit turn ۳۲)
     (ب) chat-end part09: main chat بخش‌های اصلی TODO را
          به PENDING K-section + HELPER_CONSOLIDATION_part09.md
          منتقل می‌کند
     (پ) Future improvement: TODO خود می‌تواند فایل
          claude_workspace/HELPER_SESSION_TODO.md باشد، نه
          chat artifact

F23. [NEW] Paste-Ready Verbosity vs Minimum-Necessary Tension
     paste-ready turn ۴۲ ~۹۰ خط است (بزرگ‌تر از معمول).
     این تنش بین F17 (depth verification) و سه‌قاعده پایه
     (minimum-necessary) را نشان می‌دهد. در آینده، اگر
     paste-ready خیلی بزرگ شود، می‌توان آن را به دو بخش
     تقسیم کرد: (الف) execution instructions، (ب) محتوای
     verbatim فایل‌ها در فایل جداگانه.

     Decision این turn: detail چون F17 critical.
     Future option: split اگر pattern تکرار شد.

F24. [NEW] User Pre-Merge Checkpoint Pattern (positive observation)
     در turn ۴۳، user قبل از merge updates به فایل دستی،
     تأیید گرفت. این یک defensive pattern مفید است که
     fidelity loss در external file را کاهش می‌دهد.

     این مقایسه با F11 (Race Condition) مشابه است: helper
     read-back می‌کند، user pre-merge confirm می‌کند. هر
     دو نمونه‌های verification-before-commit هستند.

     Suggestion: این می‌تواند convention استاندارد شود
     (helper proposes update → user confirms → user merges).

F25. [NEW] Helper-Side Turn Authority Confusion
     در paste-ready turn ۴۲، helper به turn‌های helper session
     (۳۴, ۴۰, ۴۲) به‌عنوان "user explicit approval" reference
     داد. این طبق HELPER_PROTOCOL §۱.۴ نادرست است — authority
     فقط در chat اصلی valid است. helper-side approval حتی اگر
     explicit باشد، advisory است.

     Lesson: paste-ready به main chat باید reference به user
     approval در helper-side نکند. به جای آن، باید بنویسد
     "این پیشنهاد helper است، نیاز به user explicit approval
     در main chat دارد".

F26. [NEW] Main Chat Decision Blindness در Helper
     در turn ۱۹ main chat، user explicit تأیید کرد:
     - minimum-necessary chat-end housekeeping only
     - NO new deliverable
     - stay on main path

     helper این DECISION 8 را نمی‌دانست (helper به turn‌های
     main chat surface ندارد). در turn‌های ۳۰-۴۲ helper،
     live_inbox setup planned شد — که DECISION 8 را override
     می‌کند.

     Lesson: helper پیشنهادات infrastructure باید explicit
     check کند که با DECISION‌های recent main chat conflict
     ندارد. user-as-router مسئول این sync است، ولی helper
     می‌تواند explicit بپرسد "آیا DECISION اخیری در main
     chat دارید که این پیشنهاد را block می‌کند؟"

F27. [NEW] Session Cross-Side Decision Sync Failure
     helper-state و main-state synchronized نیستند.
     user-as-router مسئول این sync است، ولی این burden
     مهم است.

     این مستقیماً همان scenario است که cross-chat
     communication mechanism (gozineh الف live_inbox)
     برای حل آن طراحی شد.

     ⚠️ Paradoxical: ما mechanism برای حل این مشکل را با
     ایجاد همان مشکل (DECISION 8 blindness) شروع کردیم.

F28. [NEW] Bootstrap Paradox of Cross-Chat Infrastructure
     instrument برای solving X با X خودش fail می‌کند.
     این یک M-candidate genus جدید است — مشابه HM-5
     (Self-Referential First-Application chicken-and-egg).

     Lesson: قبل از proposing infrastructure برای حل X،
     check کن که proposal خود مشکل X را reproduce نمی‌کند
     در runtime.

     Discovery #15 helper part09 (محتمل critical severity
     طبق Rule #77).

F29. [NEW] Helper Should Ask Before Infrastructure Proposals
     قبل از proposing هر infrastructure deliverable (مانند
     live_inbox)، helper باید explicit بپرسد:
     "آیا DECISION یا constraint اخیری در main chat هست
     که این پیشنهاد را block می‌کند؟"

     این یک complement به F20 (Pre-Existing User Constraint
     Blindness) است — F20 درباره session-helper history
     است، F29 درباره main-chat history.

-------

F30. [NEW] Helper Over-Verification of User Authority
    در turn ۴۵، user explicit با گزینه (ب) موافقت کرد.
    helper سپس درخواست verification ثانوی کرد ("آیا DECISION 8
    در main chat turn ۱۹ به آن کلمات بود؟").

    این redundant بود — user همان authority است در هر دو
    چت. یک‌بار approval کافی است.

    Lesson: وقتی user explicit decision می‌دهد، helper نباید
    "verification ثانوی" بخواهد. این یک نوع over-caution است
    که process را slower و more confusing می‌کند.

    این یک sibling به F6 (Verdict-Without-Analysis) از طرف
    مقابل است — این turn، helper بیش‌ازحد cautious بود، نه
    بیش‌ازحد confident.

F31. [NEW] Operational Verification Layer Missing از
    Mechanism Design

    در turn ۳۲، helper یک 4-layer mechanism ساخت
    (Triple-Anchor + File-Based + Audit Gatekeeping +
    Boot Protocol). ولی هیچ یک از آن لایه‌ها به‌طور explicit
    operational verification که mechanism اجرا شد را
    cover نمی‌کند.

    این یک gap critical است — mechanism بدون verification
    خود یک نوع از mental checking (M100) است در سطح
    architecture.

    Lesson: هر mechanism که "این کار باید بشود" را
    establish می‌کند، نیاز به layer جداگانه برای "verify
    این کار actually شد" دارد. این دو distinct هستند —
    intent vs execution.

    این observation worth M-promotion در part10 است.

Update I-section (DEFERRED to part10):
- I1-I8: همه به part10 منتقل شدند

F32. [NEW] HM-2 Escape Honored in part09 chat-end
     در turn ۴۹ helper، helper گزینه (د) defer chat-end
     را توصیه کرد بر اساس HM-2 explicit escape criteria
     (HELPER_PROTOCOL §۳.۴). این یک governance pattern
     مثبت است:
     - escape criteria explicit بود (HM-9=6 above threshold)
     - alternative (گزینه ب) با W3 conflict داشت
     - S4 deliverable از قبل safe بود (pushed)

     Lesson: HM-2 escape باید با confidence اجرا شود وقتی
     criteria explicit satisfied است، نه به‌عنوان "failure
     mode" بلکه به‌عنوان "designed safety mechanism".

     این یک observation positive است (نه catch error).

F33. [NEW] W3 Already Useful in Decision-Making
     W3 (chat-end file sync verification) که turn ۴۷ user
     قاعده‌مند کرد، در turn ۴۹ helper به‌عنوان یک criterion
     در DECISION 14 analysis استفاده شد. این تأیید می‌کند
     که W3 universal-binding بود — حتی قبل از formalization
     در part10، practically applied می‌شود.

     Lesson: قواعد universal-binding می‌توانند immediate
     useful باشند حتی قبل از formal codification.

F34. [NEW] Helper Role-Abdication Pattern (sibling به F6)
     در turn ۵۱ helper پس از 3 verdict reversal،
     responsibility decision را به user منتقل کرد ("شما
     تصمیم می‌گیرید"). این یک sibling از F6 (Verdict-
     Without-Analysis) است در طرف مخالف — F6 = over-
     confident verdict، F34 = under-confident abdication.

     هر دو violations از role professional advisor
     هستند. correct mode بین این دو است: confident
     decision-making + transparent reasoning +
     acceptance of correction.

F35. [NEW] Anti-Circular Decision Mechanisms
     در turn ۵۲ user catch درباره چرخه باطل verdict
     reversals (3 reversals در 22 turn). helper پنج
     Mechanism A-E را به‌عنوان binding rules ارائه کرد.
     این یک upstream solution است برای pattern F6
     (Verdict-Without-Analysis) و F34 (Role-Abdication)
     ترکیبی.

     Lesson: Mechanism‌های coercive (numerical ceiling،
     frozen option space، permanent criteria registry)
     قوی‌تر از intent statements هستند. درس parallel به
     M85/M87 (positive constraint > negative reminder).

F36. [NEW] Helper Self-Enforcement Failure
     در turn ۵۲، helper پنج Mechanism A-E تعریف کرد ولی
     enforcement آن‌ها را به user trigger phrases وابسته
     کرد. این C5 (HM-META-K Automation-First) را نقض
     می‌کند.

     Lesson: هر rule binding helper باید self-enforced
     باشد — یعنی helper خود در هر turn explicit check
     می‌کند، نه اینکه منتظر user reminder باشد.

F37. [NEW] Mechanism Enforcement Must Be File-Based
     پنج Mechanism A-E (turn ۵۲) intent خوبی داشتند ولی
     enforcement بد بود (user-trigger-based).

     Lesson برای part10:
     هر helper session جدید باید با یک Decision Tracking
     Registry در فایل پایدار شروع شود:
     claude_workspace/HELPER_SESSION_DECISIONS_{date}.md

     helper در شروع هر turn:
     1. read این فایل
     2. اعمال Mechanism A-E بر اساس state موجود
     3. update فایل با state جدید
     4. سپس پاسخ به user

     این یک P10-CANDIDATE قوی است برای infrastructure
     sprint part10.

F38. [NEW] Helper Persian Language Inconsistency
     در طول session part09، helper به‌طور inconsistent
     از فارسی و انگلیسی استفاده کرد. متن‌ها اغلب
     English-heavy بودند با فارسی به‌عنوان connective
     tissue.

     Lesson: per قاعده L1 جدید (turn ۵۵)، فارسی preferred
     در توضیحات regular. helper باید این را از turn بعد
     خود اعمال کند، نه فقط توصیه برای main chat.

DECISION 14 = (د) — EXECUTED ✅
     main chat turn 30: deferred note commit + push موفق
     commit: 5f3b091
     part09 session: closed clean
     S4 deliverable: safe در remote
     HM-2 escape: clean execution case study

F39. [NEW] Helper Boot Source Discipline
     در turn ۵۶، user catch درباره recursive helper-to-helper
     drift (F3, F9) و نیاز به boot از فایل پایدار به‌جای
     paste history.

     Lesson: helper بعدی باید از فایل پایدار boot کند، نه
     از paste history. این رفع ساختاری F3/F9 است.

     مکانیزم:
     - PART09_CHAT_END_DEFERRED_NOTE.txt (روی disk)
     - HELPER_SESSION_TODO_part09_final.md (روی disk)
     - Constitution v2.14 modular files
     - سند جامع v2.11

     هیچ paste از chat helper قبلی به helper جدید. این
     حلقه recursive drift را می‌شکند.

## G. SESSION META — به‌روزرسانی این TODO
────────────────────────────────────────────────────────────

G1. هر turn جدید: من check می‌کنم چه آیتم status تغییر کرد

G2. هر تغییر/افزوده: من صریحاً اعلام می‌کنم در کدام بخش/آیتم

G3. در ابتدای هر turn: من ۳-قاعده پایه را یادآوری می‌کنم

G4. در پایان هر paste-ready: ۳-قاعده پایه footer

G5. Track session meta-metrics (FINAL state):
    - turn count: 55 (helper) — final این session
    - user correction count: 19 جدی total (H1-H19)
    - HM-9 hang count: 8 cumulative (part08: 2 + part09: 6)
    - main chat correction count: 1 (turn ۲۱ main)
    - اگر metrics از threshold عبور: pre-emptive transfer
      proposal به user
    - session productivity high: 38 observations + 5 Mechanisms
      + 9 Criteria + 8 P10-CANDIDATEs

G6. F14 enforcement: ابتدای هر turn، یادآوری Rule systematic
    depth. در پایان، self-check آیا اعمال شد.

G7. Transfer consideration evaluated, declined for now
    reason: TODO v4 جامع است + Filesystem-First discipline
    ثابت شده. اگر execution turn بعد catch جدید بدهد،
    transfer revisit شود.

## H. USER CATCHES LOG (transparency برای trust)
────────────────────────────────────────────────────────────

H1. [✅] turn 26: helper fabrication catch (17 decisions,
        HM-META L-Q proposals)

H2. [✅] turn 28: document تجمیعی catch (همان pattern recurrence)

H3. [✅] turn 30: missing mechanism (M23 prevention layer)

H4. [✅] turn 32: missing TODO checklist suggestion

H5. [✅] turn 32: missing cross-chat communication suggestion

H6. [✅] turn 33: missing proactive advisor implementation

H7. [✅] turn 33: missing trade-off analysis (verdict turn 30)

H8. [✅] turn 34: «همه چیز اوکی است؟» check — F10-F13 شناسایی

H9. [✅] turn 35: تأکید روی systematic depth universal rule
        ⚠️ تعداد catch‌ها بالا (9 جدی در 30+ turn) — این
           pattern یک observation برای part10 transfer
           consideration است

H10. [✅] turn ۳۹: helper governance violation — تصمیم
        execute مستقیم در turn ۳۵ علی‌رغم constraint موجود
        "helper اجازه نوشتن/ویرایش ندارد"
        ⚠️ این H10 یک catch بسیار مهم است — helper یک
           governance قاعده pre-existing را فراموش کرد و
           با criteria چیدمان (Rule #76) decision گرفت که
           explicit با آن قاعده در تضاد بود.

H11 [✅] turn ۴۱: F20 ثبت پایدار not catch (user)
        ⚠️ user notice کرد که F20 و سایر F-items هنوز فقط
           در chat memory هستند، نه فایل پایدار. این یک
           live demonstration از M87 spirit بود.

H12. [✅] turn ۲۱ main chat: catch DECISION 8 violation + Rule #62 misapplication +

H13. [✅] turn ۴۶: user catch درباره separate code blocks
        + my over-verification (F30)
        ⚠️ این session 13 user catch داشت — high count
           اگر turn بعدی session حقیقتاً تمام شد،
           transfer به part10 helper مناسب

H14. [✅] turn ۴۷: user catch درباره chat-end file sync
        verification missing از 4-layer mechanism
        ⚠️ این critical catch بود — همان M23/M87 pattern
           در سطح architecture کشف شد

H15. [✅] turn ۲۹ main: HM-9 #6 ظاهر شد روی edit_file
        SESSION_STATUS.md
        ⚠️ این critical moment session بود — HM-2 escape
           criteria explicitly satisfied (6 HM-9 + 2 audit
           fail). منجر به DECISION 14 = (د) defer به part10 شد.

H16. [✅] turn ۵۱ user: catch درباره role-abdication
        ⚠️ critical catch — helper مسئولیت تصمیم را به
           user منتقل کرد (violation Rule #1.2 + #61).
           اصلاح in same turn پذیرفته شد.

H17. [✅] turn ۵۲: user catch درباره چرخه باطل verdicts
        پنج Mechanism A-E binding تعریف شد.

DECISION 14 = (د) defer به part10 — RE-VALIDATED با
            Mechanism A-E (همه PASS). final.

H18. [✅] turn ۵۳: user catch درباره self-enforcement
        ⚠️ Critical insight — همه Mechanism‌ها must be
           automation، نه user-trigger.

H19. [✅] turn ۵۵: user تأیید قاعده زبان فارسی universal
        ⚠️ این قاعده hardcoded در L1 آمد. universal-binding
           برای همه پروژه‌ها.

H20. [✅] turn ۵۹: user catch درباره gaps + universal
        promotion Mechanism A-E
        ⚠️ user دستور داد: (الف) gap items را اضافه کن
           (F19, F32, F33, F39, H15)، (ب) Mechanism A-E
           باید در ابتدای همه چت‌ها باشد نه فقط helper.
        این دومی یک universal promotion مهم است.

## I. EXECUTION و paste-ready ordering
────────────────────────────────────────────────────────────

I1. [DEFERRED to part10] helper execution live_inbox setup
       Status history:
       - turn 38: planned (subtasks I1.0-I1.8)
       - turn 42: helper paste-ready آماده شد
       - turn 45: DEFERRED به part10 per DECISION 8 (turn 19 main)

       Original subtasks (defer to part10):
       - I1.0 git status check + checkpoint (per F15)
       - I1.1 ایجاد claude_workspace/live_inbox/
       - I1.2 ایجاد from_main_to_helper/ sub-directory
       - I1.3 ایجاد from_helper_to_main/ sub-directory
       - I1.4 ایجاد archive/ sub-directory
       - I1.5 ایجاد conversation_log.md (linear index)
       - I1.6 ایجاد README.md (protocol + encoding per F10)
       - I1.7 read-back verification هر فایل (per F11)
       - I1.8 تأیید موفقیت با depth (per F17)

I2. [ ] turn 39 (post-I1 approval): paste-ready به main chat
       - references به فایل‌های live_inbox موجود (per F12)
       - شامل Triple-Anchor instructions (B1)
       - شامل HELPER_CONSOLIDATION_part09.md creation (B2)
       - شامل verbatim discipline برای fidelity
       - شامل ordered execution STEP 1-6 (B5)
       - شامل acceptance criteria کامل (B6, B8, B9)
       - footer سه‌قاعده پایه

I3. [ ] turn 40+: optional cross-chat test (per F18)
       - simple test message round-trip قبل از reliance بر
         mechanism در chat-end

I4. [ ] turn 41+: user paste به main chat

I5. [ ] turn 42+: main chat execution chat-end part09
       - Triple-Rule atomic per Rule #73
       - HELPER_CONSOLIDATION_part09.md write verbatim
       - audit pass 11/11
       - commit + push (Rule #66)

## W. WORKFLOW RULES
────────────────────────────────────────────────────────────

W1. [WORKFLOW] Incremental Append Pattern (turn ۴۴)
     user به‌جای merge در محل اصلی، updates را در آخر فایل
     اضافه می‌کند. در نهایت، helper تجمیع و مرتب‌سازی نهایی
     را انجام می‌دهد. این workflow:
     - speed: high (no merge cognitive load)
     - fidelity: high (همه updates در منبع واحد)
     - helper role: text generation (نه file write — همخوان
       با F20)

W2. [WORKFLOW] Separate Code Blocks for Paste-Ready vs TODO Updates
    user explicit در turn ۴۶ خواست:
    - paste-ready به main chat در یک کادر کد جدا
    - TODO updates در یک کادر کد جدا
    - این دو نباید در یک کادر یا در ترکیب باشند

    rationale: کپی-پیست راحت‌تر. user می‌تواند هر کادر را
    مستقل به destination مربوطه ببرد.

W3. [NEW WORKFLOW RULE] Chat-End File Sync Verification
    (UNIVERSAL، BINDING، NON-NEGOTIABLE)

    در پایان هر chat (main یا helper)، قبل از declare
    "chat-end complete"، Claude باید explicit verify کند:

    LEVEL 1 — File Existence Check:
    - فایل‌های لازم پروژه که در این chat-end باید آپدیت
      شوند، actually موجود هستند؟
    - method: Filesystem MCP `list_directory` یا
      `get_file_info` بر هر فایل

    LEVEL 2 — File Content Sync Check (CRITICAL):
    - اگر فایل در این chat-end آپدیت شد، آیا آپدیت
      actually در فایل روی دیسک reflect شده است؟
    - یا فقط در حافظه chat هست؟
    - method: Filesystem MCP `read_text_file` +
      verification محتوا با آنچه ادعا شد آپدیت شد

    Tail behavior:
    - اگر chat-end دوباره اجرا شد (به هر دلیل — repeat
      pass، correction، scope addition بعدی)، این چک باید
      هر بار از نو انجام شود
    - این چک "بار اول OK بود" نیست — هر execution مستقل
      verify می‌شود

    Anti-pattern blocked:
    - "فایل آپدیت شد" بدون read-back (M82 violation)
    - "همه چیز OK" claim بدون per-file verification
      (Rule #76 violation + M100)
    - assumption که "اگر قبلاً انجام شد، الان هم درست است"

    Scope:
    - main chat chat-end: UNIVERSAL
    - helper chat chat-end: UNIVERSAL
    - re-execution of chat-end (per any reason): UNIVERSAL

    Promotion candidate:
    - HELPER_PROTOCOL.md v1.1 §۸ extension
    - یا Rule جدید در 01_rules.md در part10 با proper
      Review (REVIEW_PROTOCOL trigger)

    منبع تأیید: user explicit در turn ۴۷ با emphasis
    "باید و باید و باید"

    Status: applied immediately در chat-end part09 main
    chat (per turn ۴۸ explicit ضمیمه به DECISION 12)

## L. UNIVERSAL LANGUAGE RULES
────────────────────────────────────────────────────────────

L1. [NEW UNIVERSAL RULE] Persian Language Preference

═══════════════════════════════════════════════════════════════

L2. [NEW UNIVERSAL RULE] Anti-Circular Decision Mechanisms (Universal)
═══════════════════════════════════════════════════════════
binding برای همه helper و main chat sessions
scope: همه پروژه trading-system + همه پروژه‌های آینده

این promotion از original Anti-Circular Mechanism A-E (turn ۵۲)
است. user explicit در turn ۵۹ تأیید کرد که این Mechanism‌ها
باید در ابتدای همه چت‌ها active باشند، نه فقط helper.

محتوا: 5 Mechanism A-E (در section "ANTI-CIRCULAR DECISION
MECHANISMS" در همین TODO)

Self-enforcement (per HM-META-K + F36):
   هر turn helper یا main chat باید explicit self-check
   اجرا کند:
   [Mechanism Self-Check — auto، هر turn]
   - A: تعداد reversal این decision = X/2
   - B: option جدید معرفی شد؟
   - C: criteria C1-CN applied هستند؟
   - D: HM-9 یا tool failure در 3 turn اخیر؟
   - E: reverse می‌کنم بدون trigger؟

   اگر هر check FAIL، STOP و explicit acknowledge.

Promotion candidate برای part10:
   - HELPER_PROTOCOL.md v1.1 §۸ extension (helper-side)
   - Rule جدید در 01_rules.md (main chat-side)
   - یا Custom Instructions extension (both)

منبع تأیید: user explicit در turn ۵۹ پس از part09 helper
            session — promotion از turn ۵۲ helper-only
            به universal binding

## ANTI-CIRCULAR DECISION MECHANISMS (turn ۵۲)

> binding برای همه helper sessions این پروژه و آینده
> اعمال خودکار در هر turn helper (per F36 self-enforcement)

Mechanism A — Hard Numerical Ceiling
   max 2 reversal per decision. سومی = escape signal.

Mechanism B — Option Space Frozen
   تمام گزینه‌ها upfront شناسایی شود. گزینه جدید post-hoc
   = explicit acknowledge + user approval required.

Mechanism C — Criteria Registry Permanent
   همه criteria در TODO permanent. هر decision باید
   explicit apply کند به همه criteria.

Mechanism D — Pre-Decision Tool Health Check
   HM-9 یا tool failure در 3 turn اخیر = execute-heavy
   options de-prioritize شوند.

Mechanism E — Termination Clause
   هر verdict با clause: "نهایی مگر fact جدید یا user
   explicit reconsider". reversal بدون trigger explicit
   not allowed.

User trigger phrases برای enforcement (backup به self-enforce):
- "این بار سوم reversal است" → A violation
- "این گزینه در analysis اولیه‌ات نبود" → B violation
- "C{N} را چطور apply کردی؟" → C check
- "tool health چک کردی؟" → D check
- "termination clause کجاست؟" → E enforcement

═══════════════════════════════════════════════════════════════

## CRITERIA REGISTRY (this session)

C1: constraint pre-existing user (F20)
C2: HM-9 mitigation (F11, F21)
C3: W3 chat-end file sync verification
C4: HM-2 escape criteria (HELPER_PROTOCOL §3.4)
C5: minimum-necessary principle
C6: governance hierarchy (Authority §1.4)
C7: file-grounded evidence
C8: S4 deliverable safety (234ba2f pushed)
C9: L1 Persian Language preference

═══════════════════════════════════════════════════════════════

## SELF-ENFORCEMENT TEMPLATE (turn ۵۳)

از این لحظه، helper هر turn با این self-check شروع می‌کند:

[Mechanism Self-Check — auto، هر turn]
- A (max 2 reversals): تعداد reversal این decision = X/2
- B (option space frozen): option جدید معرفی شد؟
- C (criteria registry): C1-CN applied هستند؟
- D (tool health): HM-9 یا tool failure در 3 turn اخیر؟
- E (termination clause): reverse می‌کنم بدون trigger؟

اگر هر check FAIL، helper STOP و explicit acknowledge.

═══════════════════════════════════════════════════════════════

## P10-CANDIDATEs (carry-forward to part10)

1. Helper-Sandbox Synthesis Drift (meta — recursive synthesis exponential drift)
2. Bootstrap Paradox of Cross-Chat Infrastructure (meta — instrument fails with same problem it solves)
3. Cross-Side Decision Sync Failure (process — user-as-router burden، state-of-record non-synced)
4. live_inbox setup formalization (design — deferred from part09)
5. W3 Chat-End File Sync Verification formalization (process — applied immediately، Rule promotion candidate)
6. HM-9 Tool-Level Reliability Pattern (tool/process — cumulative 8 events، Rule promotion candidate)
7. File-Based Decision Tracking Registry for helper sessions (process — automation per HM-META-K)
8. L1 Persian Language Preference formalization (universal rule)

═══════════════════════════════════════════════════════════════

## DECISION 14 — EXECUTED ✅

DECISION 14 = گزینه (د) defer chat-end به part10

main chat turn 30: deferred note commit + push موفق
commit: 5f3b091
part09 session: closed clean
S4 deliverable: safe در remote
HM-2 escape: clean execution case study

═══════════════════════════════════════════════════════════════

## PART09 SESSION FINAL STATE

Branch: infra/v2.14-source-of-truth
Final HEAD: 5f3b091 (deferred note)
Parent: 234ba2f (S4 — D12 Audit Checks)
Grandparent: 3fd2405 (part08 chat-end)

Commits این session (part09):
1. 234ba2f — feat(audit): S4 D12 (11 checks)
2. 5f3b091 — docs(part09): defer chat-end per HM-2 escape

Delivered ✅:
- D12 Audit Checks #8-11 (S4)
- Z3.15, Z3.16, Z3.17, Z3.25 RESOLVED
- Rule #74 eat-your-own-dogfood resolved
- MDRS v2: 13/24 → 14/24

Deferred to part10 ⏸️:
- Full Triple-Rule atomic chat-end (6 files)
- live_inbox setup (P10-CANDIDATE-4)
- 8 P10-CANDIDATEs total carry-forward
- W3 verification application (full)

═══════════════════════════════════════════════════════════════
## PART10 BOOT INSTRUCTIONS (Critical)
═══════════════════════════════════════════════════════════════

برای helper بعدی در part10:

⚠️ HARD RULE 1 — Anti-Recursive-Drift (per F3, F9):
   helper بعدی NEVER از helper sandbox قبلی paste-boot
   نمی‌شود. به‌جای آن:

   1. Read PART09_CHAT_END_DEFERRED_NOTE.txt (روی disk)
   2. Read این فایل: HELPER_SESSION_TODO_part09_final.md (روی disk)
   3. Read Constitution v2.14 modular files
   4. Read سند جامع v2.11
   5. شروع کار fresh با این فایل‌ها به‌عنوان source-of-truth

   هیچ paste از chat helper قبلی به helper جدید.
   این حلقه recursive drift را می‌شکند.

⚠️ HARD RULE 2 — Universal Mechanism A-E Self-Check (per L2):
   هر chat (helper یا main) باید در ابتدای هر turn
   این self-check را اجرا کند:

   - A (Hard Numerical Ceiling): max 2 reversal per decision
   - B (Option Space Frozen): گزینه‌ها upfront، جدید explicit
   - C (Criteria Registry Permanent): همه criteria applied
   - D (Pre-Decision Tool Health Check): HM-9 awareness
   - E (Termination Clause): no reverse بدون trigger

   این universal است، نه helper-only.
   refer به section L2 و ANTI-CIRCULAR section در این TODO.

═══════════════════════════════════════════════════════════════
END OF TODO CHECKLIST v6
═══════════════════════════════════════════════════════════════
